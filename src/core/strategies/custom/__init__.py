"""Custom strategies directory.

Place custom trading strategies here. They will be auto-discovered
by the StrategyRegistry if they use the @register_strategy decorator.

Example usage with git submodule:
    git submodule add git@github.com:username/private-strategies.git \\
        src/core/strategies/custom
    git submodule update --init --recursive
"""
