---
stepsCompleted:
  - step-01-document-discovery
  - step-02-prd-analysis
  - step-03-epic-coverage-validation
  - step-04-ux-alignment
  - step-05-epic-quality-review
  - step-06-final-assessment
documentsInScope:
  prd: _bmad-output/planning-artifacts/prd.md
  architecture: _bmad-output/planning-artifacts/architecture.md
  epics: _bmad-output/planning-artifacts/epics.md
  ux: null
---

# Implementation Readiness Assessment Report

**Date:** 2026-08-03
**Project:** Trading-ntrader (Phase 3 — Paper Trading)

## Step 1: Document Discovery

### PRD Files Found

**Whole Documents:**
- `prd.md` (65,560 bytes, modified 2026-07-26 13:02)

**Sharded Documents:** none

### Architecture Files Found

**Whole Documents:**
- `architecture.md` (47,245 bytes, modified 2026-07-26 13:18)

**Sharded Documents:** none

### Epics & Stories Files Found

**Whole Documents:**
- `epics.md` (91,033 bytes, modified 2026-07-26 21:12)

**Sharded Documents:** none

### UX Design Files Found

None found in `_bmad-output/planning-artifacts/`. (Phase 1 had a UX spec, archived at `_bmad-output/archive/phase-1-stocks/`; Phase 2 and this Phase 3 cycle have none.)

## Issues Found

- ⚠️ **WARNING**: No UX design document found for this cycle. Phase 3 (paper trading) appears to be primarily backend/engine work per the PRD and architecture docs, so this may be expected rather than a gap — needs your confirmation.
- No duplicates detected — each document type has exactly one whole-document version in `planning-artifacts/`, and prior phases (1 and 2) are cleanly archived under `_bmad-output/archive/`, not sitting alongside the current versions.

## Documents In Scope For This Assessment

- **PRD:** `prd.md`
- **Architecture:** `architecture.md`
- **Epics & Stories:** `epics.md`
- **UX:** none (pending your confirmation this is expected)

## PRD Analysis

### Functional Requirements

**Live Connectivity & Market Data (E1)**

FR1: System can connect to Interactive Brokers as a live data source and receive real-time bars for subscribed instruments.
FR2: System can connect to Interactive Brokers as an execution destination capable of submitting and tracking orders.
FR3: System can operate on real-time market data rather than delayed data.
FR4: System can restrict trading activity to regular trading hours, matching the session basis of the backtest data.
FR5: System can maintain live broker connections without conflicting with a historical-data client operating concurrently.
FR6: System can detect loss of broker connectivity and refrain from submitting orders while disconnected.
FR7: System can run a live trading process alongside the existing backtest and data-fetch components without trading-framework initialization conflicts.

**Trading Safety & Account Gating (E1)**

FR8: System can determine whether the configured broker connection targets a paper account or a real-money account.
FR9: System can refuse to start a session against a real-money account, before establishing any connection and before any order can be submitted.
FR10: Operator can deliberately authorize real-money trading through an explicit mechanism that cannot be triggered by configuration error alone.
FR11: System can report a safety refusal distinguishably from other startup failures.
FR12: System can enable order submission for sessions permitted to trade, while defaulting to non-trading operation.

**Session Definition & Lifecycle (E2)**

FR13: Operator can define a trading session comprising one or more strategy specifications, each with its own parameters and target instruments.
FR14: System can persist a session's specification immutably at creation, so it cannot change over the session's life.
FR15: Operator can associate a session with the backtest it is intended to be compared against.
FR16: Operator can start a session by reference to its identifier, without re-specifying its parameters.
FR17: Operator can stop a running session without ending it, leaving it resumable.
FR18: System can stop a session without closing or modifying open positions.
FR19: System can preserve session identity across any number of stop/start cycles.
FR20: System can represent session state distinctly across created, running, stopped, and sealed conditions.
FR21: Operator can list all sessions with their current state and accumulated trade count.
FR22: Operator can view an individual session's state, trade count, open positions, and last activity.

**Order Execution & Trade Capture (E3)**

FR23: System can submit orders generated by strategy logic to the broker.
FR24: System can track the full lifecycle of a submitted order — acceptance, partial fill, complete fill, rejection, cancellation, and expiry.
FR25: System can aggregate multiple partial fills into a single position using volume-weighted prices.
FR26: System can capture order rejections together with the reason reported by the broker.
FR27: System can prevent submission of a duplicate order for a request already working at the broker.
FR28: System can persist each completed trade at the time it closes, rather than deferring persistence to session end.
FR29: System can record broker-charged commissions on each trade as actually incurred.
FR30: System can apply whole-share position sizing for equity and ETF instruments.
FR31: System can continue operating when an order is rejected, without silently ceasing to trade.

**State Reconciliation & Resume (E4)**

FR32: System can retrieve the broker's authoritative view of positions and cash for the configured account.
FR33: System can reconcile internal order and position state against the broker's state at session start, before any strategy begins trading.
FR34: System can maintain alignment between internal state and broker state throughout a running session.
FR35: System can resolve any conflict between internal state and broker state in favor of the broker.
FR36: Operator can request an on-demand reconciliation check and receive an explicit report of any discrepancy found.
FR37: System can restore strategy indicator state from historical data before that strategy begins processing live data.
FR38: System can resume a strategy mid-position, such that it continues seeking exits and stop levels for positions opened during a prior process run.
FR39: System can absorb position and cash changes arising from corporate actions via broker-authoritative state.

**Session Sealing & Comparison (E5)**

FR40: Operator can seal a stopped session, ending it permanently and producing a results record.
FR41: System can compute a sealed session's performance metrics using the same calculation path used for backtests.
FR42: System can persist a sealed session's results in the same form as backtest results, without a parallel results structure.
FR43: System can record the closed-trade count with a sealed session's results.
FR44: System can distinguish a paper-trading record from a backtest record.
FR45: System can record a session's execution conditions with its results — starting capital, trading-hours policy, price-adjustment basis, and market data tier.
FR46: Operator can view a sealed session in the existing results list, detail, and comparison views alongside backtests.
FR47: System can handle open positions at seal time explicitly and report their disposition.

**Observability & Diagnostics (cross-cutting)**

FR48: System can report session activity as structured streaming output during execution — bars processed, signals, orders, fills, rejections, and reconnections.
FR49: Operator can determine why a session stopped trading or terminated.
FR50: System can isolate a failure in one strategy from other strategies within the same session.
FR51: Operator can obtain machine-readable output for session status and listing.

**Configuration (cross-cutting)**

FR52: Operator can supply broker connection settings and credentials via typed environment-based configuration, never in the session specification.
FR53: Operator can configure which instruments and strategy parameters a session trades at creation time.

**Total FRs: 53**

PRD-stated journey coverage map (for later traceability cross-check): J1 → FR1–7, 23–30 · J2 → FR17–19, 32–38 · J3 → FR32–36 · J4 → FR8–12 · J5 → FR26, 28, 31, 48–50 · J6 → FR40–47.

### Non-Functional Requirements

The PRD does not number NFRs individually — it groups them under eight category headings. I've assigned sequential IDs per category (prefix = category initial) to support traceability; these are my extraction labels, not PRD-native numbering.

**Performance & Latency**

NFR-P1: Bar close → order submission in < 1 second (observable latency relative to shortest traded timeframe, not a hard target).
NFR-P2: Bar processing throughput — no accumulating backlog; each bar's processing completes before the next arrives at the shortest supported timeframe.
NFR-P3: Session startup (connect → reconcile → warm indicators → subscribe) — a session started 5 minutes before the open is trading at the open.
NFR-P4: Reconnection after a transient disconnect in < 60 seconds.
NFR-P5: Reconciliation check completes in < 30 seconds.
(No target stated for sealing or session listing — infrequent, operator-initiated, not on the critical path.)

**Reliability**

NFR-R1: Duplicate orders: exactly 0 — an invariant, not a target.
NFR-R2: Unattended uptime — a session sustains a full RTH trading day (6.5 hours) without operator intervention.
NFR-R3: Zero trade loss on abrupt termination (SIGKILL, power loss) — every closed trade is present in the database.
NFR-R4: Resume correctness — internal position state matches the broker's exactly (0 discrepancy) before any strategy is permitted to trade.
NFR-R5: No blind trading — zero orders submitted while the execution client is disconnected or reconciliation is incomplete.
NFR-R6: Always-valid session state — an interrupted session is always in a state that can be started or sealed.
NFR-R7: Failure isolation — a failure in one strategy does not terminate the session or affect other strategies within it.
NFR-R8: Rejections never silence a session — logged with venue reason, session continues operating.
NFR-R9: No artificial trades — no stop, restart, disconnect, or reconnect generates an entry or exit the strategy did not request.

**Integration (IBKR)**

NFR-I1: Rate-limit compliance — existing 45 req/s discipline governs live requests; historical warm-up requests respect IBKR pacing rules.
NFR-I2: Market data line budget — instrument count stays within the account's concurrent subscription limit; exceeding it fails at startup with a clear message rather than silently dropping subscriptions.
NFR-I3: Client ID isolation — the live session never contends with the historical-data client for a client ID.
NFR-I4: Reconciliation is mandatory, not optional — every session start reconciles against the broker; no path starts trading on unverified state.
NFR-I5: Gateway restart tolerance — the daily restart/auto-logoff is handled as an expected event, not an error condition.
NFR-I6: Safe degradation — when IBKR is unreachable, the session halts trading safely and reports it; never estimates, assumes, or proceeds on stale state.

**Observability**

NFR-O1: Every order is traceable end to end — submission, acknowledgement, fills, final state, and venue reason on rejection.
NFR-O2: Session-scoped correlation — all structured log output carries the session identifier.
NFR-O3: Status without log archaeology — current session state, trade count, open positions, and last activity are answerable from a status query alone.
NFR-O4: Silence is distinguishable from death — a running-but-not-trading session is reported differently from a stopped one.

**Security**

NFR-S1: Credential hygiene — connection settings/credentials exclusively via typed environment configuration; never in a session specification, never committed, never logged.
NFR-S2: Account identifiers are not logged in full.
NFR-S3: Elevated blast radius acknowledged — same credentials now authorize order submission; `ibkr_read_only=False` only ever set for a session that has passed the paper/live gate.
NFR-S4: No authentication or authorization on the localhost CLI — consistent with Phases 1–2.

**Capacity (in place of Scalability)**

NFR-C1: One concurrent session this phase — multi-session is Growth, a deliberate non-goal.
NFR-C2: Instrument count within one session is bounded by the account's market data lines, not by system design.
NFR-C3: No horizontal scaling, no multi-node, no distributed state — Redis cache serves persistence, not distribution.

**Testability**

NFR-T1: Automated tests never touch a real broker — session lifecycle, order-state handling, duplicate prevention, and reconciliation logic tested against broker doubles across unit/component/integration (`--forked`) tiers.
NFR-T2: Broker-dependent behavior verified by operator-run procedures against the real paper account — connectivity, safety gate refusal, a complete round trip, restart resume, reconciliation. Phase-gate evidence; cannot live in CI.
NFR-T3: The safety gate has an automated test, since it must hold regardless of environment.

**Maintainability (project standard)**

NFR-M1: Conforms to existing standards — Python 3.11+ with type hints, `ruff` + `mypy` gates, file/function/class size limits, TDD with the established test pyramid, >80% coverage on `src/core` + `src/strategies`.

**Total NFRs: 35** (5 Performance + 9 Reliability + 6 Integration + 4 Observability + 4 Security + 3 Capacity + 3 Testability + 1 Maintainability)

Explicitly and deliberately omitted by the PRD: **Scalability** (one operator, one session, no growth curve) and **Accessibility** (no new UI ships this phase).

### Additional Requirements

Constraints, assumptions, and technical requirements not labeled FR/NFR but load-bearing for coverage validation:

- **One Engine Framework constraint:** Live trading must use Nautilus's own live components (`TradingNode` + IBKR data/execution clients) — no second execution path, no live-specific strategy variant, no duplicate metric code. Any live-specific logic that can't route through Nautilus abstractions must be escalated, not worked around.
- **Command structure:** `ntrader live` Click group with `create`, `start <session-id>`, stop (Ctrl-C), `status [<session-id>]`, `list`, `reconcile <session-id>`, `seal <session-id>`.
- **5th Alembic migration required** for session metadata and the paper/backtest distinction.
- **Dual-repository rule applies** — DB features must land in both async (web) and sync (CLI) repositories, even though only the CLI writes this phase.
- **State ownership split:** Nautilus engine state → Redis; session metadata → PostgreSQL; ground truth for positions/cash → IBKR.
- **Open decision explicitly deferred to architecture:** disposition of open positions at seal time (orphan vs. flatten) — PRD requires only that the behavior be explicit and reported, never silent. **Must verify this was actually resolved in `architecture.md`.**
- **Deferred to the real-money gate (recorded, not addressed this phase):** Pattern Day Trader restrictions, Reg T margin, short-sale locate/borrow.
- **Out of scope (personal tool):** KYC/AML, PCI-DSS, GDPR, SOX, regulator-facing audit trails, fraud prevention, multi-user data protection, CLI/UI authentication.
- **Deliberately absent (Growth scope, not this phase):** trade-count auto-seal, divergence tolerance bands, live session monitoring UI, multi-strategy execution, session health alerting.
- **Architecture-phase version check flagged by the PRD itself:** whether `TradingNode` or a newer `LiveNode` builder API applies, depending on the pinned `nautilus-trader` version — **must verify this was resolved in `architecture.md`.**

### PRD Completeness Assessment

Initial read — thorough and unusually traceability-conscious for a document at this stage:

- Every FR is grouped under its owning epic (E1–E5) and cross-referenced against the six user journeys via an explicit coverage table (Section "Coverage & Deliberate Exclusions") — this gives step 3 a PRD-native starting point rather than one I have to infer.
- NFRs are organized by category but not individually numbered; I've imposed IDs above purely for this assessment's traceability needs.
- Two items are explicitly flagged by the PRD as **open decisions pushed to the architecture phase** (open-position-at-seal policy; TradingNode vs. LiveNode API choice) — these are not gaps in the PRD, they're deliberate handoffs, but they make `architecture.md` responsible for closing them. Step 4 (architecture analysis) needs to confirm both were actually resolved, not just carried forward silently.
- Scope boundaries (Growth/Vision, deliberate exclusions) are explicit and enumerated, reducing the risk of scope ambiguity feeding into epic drift.
- No internal FR numbering gaps or duplicates found (FR1–FR53 sequential, no skips).

## Epic Coverage Validation

### Method

`epics.md` reproduces the PRD's FR/NFR text verbatim in its own "Requirements Inventory" section
(so there is no drift between what the PRD states and what the epics document worked from), then
provides its own "FR Coverage Map" claiming 53/53 FRs mapped. Rather than accept that self-report,
I independently walked every one of the 35 stories across all 5 epics and matched each FR number
against the specific acceptance criterion that discharges it.

### FR Coverage Matrix (independently verified against story-level acceptance criteria)

| FR Range | Epic | Verified against | Status |
|---|---|---|---|
| FR1, FR3, FR4 | Epic 1 | Story 1.5 (real-time RTH bars, market-data-line budget) | ✓ Covered |
| FR2 | Epic 1 | Story 1.3 (TradingNode exec client config) — connection only; order tracking exercised in E3 | ✓ Covered (noted split) |
| FR5 | Epic 1 | Story 1.2 (`ibkr_live_client_id` validator) | ✓ Covered |
| FR6 | Epic 1 | Story 1.6 (connection-loss detection, trading-permitted flag) | ✓ Covered |
| FR7 | Epic 1 | Story 1.3 (LogGuard coexistence, `--forked` test) | ✓ Covered |
| FR8 | Epic 1 | Story 1.1 (static gate) + Story 1.4 (post-connect account verification) | ✓ Covered |
| FR9 | Epic 1 | Story 1.1 (pre-connection refusal) + Story 1.4 (post-connect shutdown) | ✓ Covered |
| FR10 | Epic 1 | Story 1.1 (`--real-money` + `NTRADER_REAL_MONEY_ACCOUNT`, both required) | ✓ Covered |
| FR11 | Epic 1 | Story 1.7 (exit code 3 vs. 4) | ✓ Covered |
| FR12 | Epic 1 | Story 1.3 (`ibkr_read_only=False` only after gate passes) | ✓ Covered |
| FR13, FR52, FR53 | Epic 2 | Story 2.1 (`SessionSpec` as list of `StrategySpec`, no connection fields) | ✓ Covered |
| FR14, FR15 | Epic 2 | Story 2.2 (migration, immutable `spec` JSONB, `linked_backtest_run_id`) | ✓ Covered |
| FR16 | Epic 2 | Story 2.5 (`live start` loads stored spec by ID) | ✓ Covered |
| FR17, FR19 | Epic 2 | Story 2.6 (graceful stop, identity survives cycles) | ✓ Covered |
| FR18 | Epic 2 | Story 2.6 (no position side effect on stop) — **actual `on_stop()` strategy fix ships in Epic 3 Story 3.1**, explicitly flagged by the epics doc itself as a cross-epic split | ✓ Covered (documented dependency, not a gap) |
| FR20 | Epic 2 | Story 2.1 (`SessionStatus` enum) + Story 2.3 (`SessionService.transition()`) | ✓ Covered |
| FR21, FR22 | Epic 2 | Story 2.8 (`live status`, `live list`, derived health) | ✓ Covered |
| FR48 | Epic 2 | Story 2.5 (session-scoped structured logs) — extended with order/fill events in E3 | ✓ Covered |
| FR49, FR50 | Epic 2 | Story 2.7 (exception isolation) + Story 2.8 (health incl. `stale`) | ✓ Covered |
| FR51 | Epic 2 | Story 2.8 (`--json` on status/list) | ✓ Covered |
| FR23, FR30 | Epic 3 | Story 3.2 (`submit_order` through live exec client, whole-share sizing) | ✓ Covered |
| FR24, FR26 | Epic 3 | Story 3.3 (full order lifecycle, venue rejection reason) | ✓ Covered |
| FR25, FR29 | Epic 3 | Story 3.5 (volume-weighted fills, commission aggregation) | ✓ Covered |
| FR27 | Epic 3 | Story 3.4 (query-don't-retry, deterministic client order IDs) | ✓ Covered |
| FR28 | Epic 3 | Story 3.6 (incremental persistence on position-closed) | ✓ Covered |
| FR31 | Epic 3 | Story 3.7 (session continues after rejection) | ✓ Covered |
| FR32 | Epic 4 | Story 4.1 (retrieve broker positions/cash) | ✓ Covered |
| FR33, FR35 | Epic 4 | Story 4.2 (startup reconciliation before trading, broker wins conflicts) | ✓ Covered |
| FR34 | Epic 4 | Story 4.3 (continuous reconciliation) | ✓ Covered |
| FR36 | Epic 4 | Story 4.6 (`live reconcile`, explicit discrepancy report) | ✓ Covered |
| FR37 | Epic 4 | Story 4.4 (indicator warm-up before live subscription) | ✓ Covered |
| FR38 | Epic 4 | Story 4.5 (mid-position resume) | ✓ Covered |
| FR39 | Epic 4 | Story 4.7 (corporate actions absorbed via reconciliation) | ✓ Covered |
| FR40 | Epic 5 | Story 5.3 (`live seal`, requires stopped session) | ✓ Covered |
| FR41 | Epic 5 | Story 5.1 (widened `ResultsExtractor`) + Story 5.2 (equity reconstruction) | ✓ Covered |
| FR42 | Epic 5 | Story 5.3 (no parallel results structure; AR43 rejects any new metrics table in review) | ✓ Covered |
| FR43, FR45 | Epic 5 | Story 5.4 (`session_conditions` in `config_snapshot`) | ✓ Covered |
| FR44 | Epic 5 | Story 5.3 (`run_type='paper'`) | ✓ Covered |
| FR46 | Epic 5 | Story 5.6 (renders unmodified in existing list/detail/comparison views) | ✓ Covered |
| FR47 | Epic 5 | Story 5.5 (refuse-by-default / `--force` open-trade-with-null-exit) | ✓ Covered |

**Result: 53/53 FRs independently confirmed covered by a specific story's acceptance criteria** —
the epics document's own coverage claim holds up under verification, not just self-report.

### Missing Requirements

None. No PRD FR is absent from the epics document, and no FR in the epics document's inventory is
absent from the PRD (the two lists are textually identical, confirming the epics doc worked from
this exact PRD rather than a stale or divergent copy).

### Additional Observation: NFR Traceability (bonus check, beyond this step's FR-only mandate)

All 35 NFRs are referenced at least once in the epics document. Cross-checking against story-level
acceptance criteria (not just the epic-level "Covers:" roll-up lines) found:

- **31 of 35 NFRs** are cited by number in a specific story's acceptance criteria (e.g., NFR1 in
  Story 3.2, NFR9 in Story 4.2/4.5).
- **4 NFRs** (NFR17 client-ID isolation, NFR28 no-auth-on-CLI, NFR29 one-concurrent-session,
  NFR31 no-horizontal-scaling, NFR35 maintainability standards — technically 5, see below) appear
  only in an epic's "Covers:" summary line without a dedicated AC citation. On inspection this is
  appropriate rather than a gap: these are either non-goals satisfied by not building the excluded
  thing (NFR28, NFR29, NFR31), a blanket project-wide lint/coverage standard enforced across all
  stories rather than one (NFR35), or substantively implemented under a different citation tag —
  NFR17's client-ID isolation is exactly what Story 1.2 builds, just tagged `(FR5, AR18)` in that
  AC rather than `(NFR17)`.
- **Minor documentation inconsistency (low severity, cosmetic):** epic-level "Covers:" lines omit
  NFR2, NFR4, NFR30, and NFR33 even though each is cited by number inside a story AC within that
  same epic (NFR2 → Story 2.5, NFR4 → Story 1.6, NFR30 → Story 1.5, NFR33 → Story 4.7). The
  requirement itself is covered; only the epic-summary roll-up line under-reports it. Worth a
  one-line fix in `epics.md` for future readers scanning the summary lines rather than the stories,
  but does not block implementation.

### Coverage Statistics

- Total PRD FRs: 53
- FRs covered in epics (independently verified): 53
- Coverage percentage: **100%**
- Total PRD NFRs: 35
- NFRs referenced in epics: 35 (100%), of which 31 have a story-level AC citation and 4 are
  satisfied structurally/as blanket standards (see above)

## UX Alignment Assessment

### UX Document Status

**Not Found** — confirmed in Step 1 document discovery, no `*ux*.md` whole or sharded document
exists in `planning-artifacts/`.

### Is UX Implied But Missing? — No

This is not a silent gap. All three documents independently and consistently state that **zero UI
work is a deliberate success criterion for this phase**, not an omission:

- **PRD** ("CLI + Web App Specific Requirements"): *"Web (none this phase): the UI participates
  only passively... Zero UI work is required, and that is a success criterion, not an omission."*
- **Architecture** (`architecture.md:298-302`, "Frontend Architecture"): *"None this phase — zero
  UI change is an E5 success criterion. Sealed sessions render through the existing
  list/detail/comparison views purely because they are ordinary `backtest_runs` rows."*
- **Epics** (`epics.md`, "UX Design Requirements"): *"None — deliberately... Zero UI work is a
  success criterion, not an omission"* — and ties the only UI-adjacent obligation to a **negative**
  one: Story 5.6 verifies sealed sessions render correctly in the existing views **without
  modifying them** (FR46, AR44).

This is architecturally well-founded, not just asserted: paper sessions reuse the exact same
`backtest_runs` / `performance_metrics` / `trades` schema and `ResultsExtractor` path as backtests
(FR41, FR42), so the existing Jinja2/HTMX list, detail, and comparison views render them without
any template, route, or query change. There is no new UI surface for a UX spec to describe.

### Alignment Issues

None. All three planning documents agree, and the mechanism that makes zero-UI-change true (schema
and metrics-path reuse) is specified consistently across PRD, Architecture, and Epics — not just
asserted in one place and assumed elsewhere.

### Warnings

None. The one thing worth carrying forward (not a gap, a scope-discipline note): `epics.md` AR44
explicitly lists `src/api/**`, `templates/**`, `src/services/backtest_query.py`, and the comparison
views as **untouched success criteria** — any diff touching them during implementation should be
treated as a criterion violation, per the epics document's own instruction, and Story 5.6's
acceptance criteria already test for this directly.

## Epic Quality Review

Applied rigorously against create-epics-and-stories standards: user-value framing, epic
independence (no epic may require a *later* epic to function), no forward dependencies at the
story level, proper story sizing, and Given/When/Then acceptance-criteria quality.

### A. User Value Focus — PASS, all 5 epics

| Epic | Title | Verdict |
|---|---|---|
| 1 | Live Broker Connection & Real-Money Safety Gate | User-facing capability + operator-facing safety journey (PRD Journey 4). Not a bare "Infrastructure Setup" — framed and delivered around what the operator can do (`ntrader live check`, refusal behavior) |
| 2 | Paper Trading Sessions That Outlive the Process | Clearly operator-outcome framed |
| 3 | Live Order Execution & Trade Capture | Clearly operator-outcome framed (a real order fills) |
| 4 | Stop, Restart & Resume Mid-Position | Clearly operator-outcome framed (survives daily restarts) |
| 5 | Seal a Session & Compare It Against Its Backtest | Clearly operator-outcome framed — the phase's payoff |

No epic is a disguised technical milestone ("Database Setup," "API Development"). Where a story is
technical in nature (e.g., 1.2 client-ID isolation, 2.1 session domain model), each still carries an
explicit "As the operator... so that..." framing tied to a concrete operator benefit, not a bare
technical task.

### B. Epic Independence — PASS, with dependencies correctly directional

Rule: Epic N must never *require* Epic N+1 to function. Checked the stated dependency chain
(E1 → E2 → E3 → E4 → E5) against each epic's own "Standalone deliverable":

- **Epic 1** stands alone: connects, streams bars, refuses a live account. No orders exist — by
  design, not by gap.
- **Epic 2** stands alone using only Epic 1: a session runs, observes bars, stops, restarts. "Still
  no orders" is stated as the deliverable, confirming no dependency on Epic 3.
- **Epic 3** depends on Epic 2's `trader_id` (Redis cache namespace) for deterministic client order
  IDs — a **backward** dependency (E3 needs E2, not the reverse), explicitly called out by the
  document itself as "the one hard cross-epic ordering constraint in the phase." This is the
  correct direction and is transparently documented, not a violation.
- **Epic 4** depends on Epic 3 (needs trades/positions to resume into) — again backward, correctly
  directional.
- **Epic 5** depends on Epic 4 existing in the roadmap, but no story in Epic 5 actually calls
  reconciliation/resume code — the dependency is narrative/business sequencing (don't seal before
  the session has proven it survives a restart), not a technical coupling. Not a violation.

No case found where an earlier epic's own deliverable is contingent on a later epic shipping first.

### C. Story-Level Forward Dependencies — 1 Minor Finding

Walked story-to-story ordering within all 5 epics; ordering is consistently sequential (each story
builds only on earlier stories in the same epic or earlier epics). One item worth flagging:

- **🟡 Minor — Story 2.5** includes explicit no-op placeholders for the `reconcile` and `warmup`
  startup phases, which are Epic 4's responsibility: *"the phases not yet implemented in this epic
  ... are present as explicit no-op placeholders that log their phase, so Epic 4 fills them in
  without reordering the sequence."* This technically references functionality not yet built in a
  later epic. In substance it is sound architecture (the ordered-phase startup sequence is a
  cross-cutting invariant worth fixing once, per AR39), and Story 2.5 remains fully completable and
  testable on its own with the no-ops in place — it does not *wait* on Epic 4. Classified as minor
  because the pattern is self-contained and explicitly justified, not a functional block.

### D. Database/Entity Creation Timing — 1 Minor Finding (self-acknowledged exception)

Standard: tables should be created by the story that first needs them, not all upfront.

- **🟡 Minor — Story 2.2's single migration** creates the *entire* phase's schema changes in one
  Alembic migration: `trading_sessions` (including the G1 heartbeat columns), `backtest_runs.run_type`
  (not consumed until Epic 5), `trades.session_id` (not exercised until Epic 3), and the
  `trades.backtest_run_id` nullable+CHECK change. This is a genuine deviation from the "create
  columns only when first needed" standard, but it is **explicitly self-acknowledged and reasoned**
  in the epics document itself: splitting into three migrations would create three chances to get
  the CHECK constraint's ordering wrong on a table the live backtest path actively queries — the
  deliberate exception is attributed to the Architecture phase's delta tree, not an oversight. I
  agree the reasoning holds (CHECK-constraint ordering risk on a hot table is a legitimate reason
  to batch), so this is a documented, justified exception rather than a defect — flagged per this
  step's instruction to check timing, not as something requiring rework.

### E. Acceptance Criteria Quality — PASS, notably strong

All 35 stories use consistent Given/When/Then structure. Spot-checked across all 5 epics for the
standard failure modes:

- **Vagueness:** none found. Every AC names a specific file, function, exit code, log event, column,
  or numeric threshold (e.g., "exit code 3, distinct from every other failure," "`is_live` count
  remains 0," "reconnection completes within 60 seconds").
- **Missing error conditions:** none found. Rejections, disconnects, SIGKILL, stale heartbeats,
  illegal state transitions, duplicate-order ambiguity, corporate actions, and zero-trade seals are
  all explicitly covered with their own ACs, not left implicit.
- **Non-measurable outcomes:** none found. Even soft requirements (e.g., NFR1's <1s latency) are
  tied to an explicit logged/observable measurement rather than an unverifiable claim.
- **Testability:** every story names its test tier and, in several cases, the exact test file
  (e.g., `tests/unit/core/test_live_gate.py`, `tests/component/test_live_trade_recorder.py`),
  consistent with the project's TDD discipline.

### F. Special Implementation Checks

- **Starter template:** N/A — Architecture correctly specifies none (AR1: brownfield, feature
  branch `015-paper-trading`, no project-initialization story). Consistent with the checklist's
  brownfield expectations.
- **Brownfield indicators present:** integration points with the existing system are explicit
  throughout (reuse of `StrategyRegistry`, `StrategyFactory`, `ResultsExtractor`,
  `BacktestPersistenceService`, existing repositories, existing CLI conventions) and the one
  migration is treated as a compatibility-preserving change with explicit "existing rows/queries
  unaffected" acceptance criteria (Story 2.2). No greenfield-only artifacts (no CI/CD setup story,
  no dev-environment story) were incorrectly included.

### Compliance Checklist (per epic)

| Epic | User value | Independent | Stories sized | No forward deps | Tables when needed | Clear ACs | FR traceability |
|---|---|---|---|---|---|---|---|
| 1 | ✓ | ✓ | ✓ | ✓ | N/A (no migration) | ✓ | ✓ |
| 2 | ✓ | ✓ | ✓ | 🟡 (2.5 placeholders) | 🟡 (whole-phase migration, justified) | ✓ | ✓ |
| 3 | ✓ | ✓ | ✓ | ✓ | N/A | ✓ | ✓ |
| 4 | ✓ | ✓ | ✓ | ✓ | N/A | ✓ | ✓ |
| 5 | ✓ | ✓ | ✓ | ✓ | N/A | ✓ | ✓ |

### Overall Severity Summary

- 🔴 **Critical Violations:** 0
- 🟠 **Major Issues:** 0
- 🟡 **Minor Concerns:** 2 (both in Epic 2, both explicitly self-documented in `epics.md` with sound
  reasoning — not oversights): the Story 2.5 no-op phase placeholders, and the whole-phase migration
  in Story 2.2.

This epic breakdown is unusually rigorous for a document at this stage — consistent with it having
already been produced through a standards-applying workflow. No remediation is required before
implementation; the two minor items are worth knowing about but do not block Phase 4.

### Closing the Loop: PRD-Flagged Open Decisions

Step 2 flagged two items the PRD explicitly deferred to the architecture phase, marking them as
requiring verification rather than assuming they were resolved. Confirmed against `architecture.md`
directly:

- **TradingNode vs. LiveNode API** — resolved at `architecture.md:158-161`: *"`TradingNode` is the
  live host API at this version. `LiveNode` does not exist in 1.220.0... Decision: `TradingNode` +
  `TradingNodeConfig`."*
- **Open positions at seal time** — resolved at `architecture.md:326-330`, explicitly labeled
  *"D6 — Seal-time open positions (PRD-assigned open decision — RESOLVED)"*: refuse by default,
  `--force` persists open trades with a null exit (the same shape backtests already use), never
  flattens.

Both PRD-flagged handoffs were genuinely closed, not silently dropped or left implicit — a good
signal of architecture-phase discipline.

## Summary and Recommendations

### Overall Readiness Status

**READY**

### Critical Issues Requiring Immediate Action

None found. Zero critical violations, zero major issues across FR/NFR extraction, epic coverage
validation, UX alignment, and epic/story quality review.

### All Findings (for completeness — none block implementation)

1. 🟡 **Cosmetic — NFR traceability under-reporting in epic summaries.** `epics.md`'s epic-level
   "Covers:" roll-up lines omit NFR2, NFR4, NFR30, and NFR33, even though each is cited by number
   inside a specific story's acceptance criteria within that same epic (NFR2 → Story 2.5, NFR4 →
   Story 1.6, NFR30 → Story 1.5, NFR33 → Story 4.7). The requirement is genuinely covered; only the
   summary line under-represents it for a reader scanning just the roll-ups.
2. 🟡 **Minor — Story 2.5 forward-references Epic 4's phases** as explicit no-op placeholders
   (`reconcile`, `warmup`). Architecturally sound (fixes the startup-phase sequence once, per AR39)
   and the story remains independently completable and testable with the no-ops in place — not a
   functional block, just worth knowing the pattern exists.
3. 🟡 **Minor — Story 2.2 bundles the whole phase's schema changes into one migration**, including
   columns (`run_type`, `trades.session_id`) not consumed until Epic 3 and Epic 5. This deviates
   from the "create only what's needed per story" standard, but is explicitly self-justified in
   `epics.md` by a specific technical risk (three migrations touching a live backtest table would
   be three chances to get the CHECK constraint's ordering wrong) and is attributed to a deliberate
   Architecture-phase call. Reasoning holds; flagged for visibility, not remediation.
4. ℹ️ **No UX document exists — confirmed deliberate, not a gap.** PRD, Architecture
   (`architecture.md:298-302`), and Epics all independently and consistently state that zero UI
   work is a stated success criterion this phase, backed by an architectural mechanism (schema and
   `ResultsExtractor` reuse) rather than just an assertion.

### Recommended Next Steps

1. **Proceed to Phase 4 implementation** — no blocking gaps were found in requirements coverage,
   epic/story structure, or cross-document alignment.
2. **Optional polish (non-blocking):** add NFR2, NFR4, NFR30, and NFR33 to their respective epics'
   "Covers:" summary lines in `epics.md` so the roll-up view matches what the stories actually test —
   a one-line documentation fix, purely for future readers who scan summaries rather than full
   stories.
3. **Carry forward during implementation:** treat `AR43`'s "Anti-patterns to reject in review" list
   and `AR44`'s "explicitly untouched" file list as living review checklists for every PR in this
   phase — they are already in the epics document and specifically designed to catch the failure
   modes this phase cares most about (a second results vocabulary, accidental `on_stop()` flattening,
   retry-on-timeout around orders, and any diff touching the zero-UI-change surface).

### Final Note

This assessment reviewed the PRD (53 FRs, 35 NFRs), Architecture, and Epics/Stories documents for
Phase 3 (Paper Trading) and found **0 critical and 0 major issues**. Three minor/cosmetic items were
identified, all self-documented or easily fixed, none blocking. FR coverage was independently
verified at 53/53 (100%), not accepted on the epics document's self-report alone. Both PRD-flagged
architecture handoffs were confirmed closed. This is one of the stronger planning sets to pass
through this assessment — proceed to implementation as-is, or apply the optional polish above first.

---

**Assessment completed:** 2026-08-03
**Assessor:** Claude (BMAD Implementation Readiness workflow)
**Documents assessed:** `prd.md`, `architecture.md`, `epics.md`
