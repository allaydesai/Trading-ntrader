# generated: 2026-08-03
# last_updated: 2026-08-03 (Phase 3 sprint board generated from planning-artifacts/epics.md — Paper Trading. 5 epics, 35 stories, all backlog; no story files created yet. Phase 1 (Stocks) and Phase 2 (ETFs) sprint boards are archived under archive/phase-1-stocks/ and archive/phase-2-etfs/. NOTE: epic-1..epic-5 keys are REUSED for Phase 3 paper-trading work — they are NOT the completed Phase 1/2 epics; those done-records live in the archive + epic-N-retro-*.md files.)
# project: Trading-ntrader
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-08-03
last_updated: >-
  2026-08-21 (Story 2.5 code-reviewed -> done, closing it. epic-2 remains in-progress:
  2.1/2.2/2.3/2.4/2.5 done, 2.6-2.8 backlog. Three adversarial layers (Blind Hunter, Edge Case
  Hunter, Acceptance Auditor) produced 30 raw / 25 deduplicated findings: 2 decisions, 13 patches,
  3 deferred, 7 dismissed. BOTH DECISIONS WERE HIGHs FOUND INDEPENDENTLY BY TWO LAYERS, both in the
  reclaim design, and Allay ruled fix-now on both. (1) The stop path had NO ownership guard:
  mark_stopped -> transition(to=STOPPED) never compared last_started_at, so a dispossessed
  incumbent exiting inside the detection window would have transitioned the SUCCESSOR'S running row
  to stopped -- and the successor would then kill itself at its next heartbeat. Fixed by arming the
  same reclaim guard the heartbeat uses (a shared _refuse_if_reclaimed, an optional started_at on
  transition() -- a deliberate widening of Story 2.3's surface, flagged for the retro), translated
  to SessionReclaimedError by the adapter and honoured by release_record and _release_quietly.
  (2) Legitimate startup silence EXCEEDED the 90s staleness threshold: the claim stamps
  last_heartbeat_at once, and the first steady write landed only after all eight phases (the 120s
  connect budget alone) plus a leading 30s sleep -- so a parallel `live start` could LEGITIMATELY
  reclaim a healthy, still-starting session, the exact NFR6 two-processes shape. Fixed with
  StartupHeartbeat, a bounded-join daemon THREAD (the long phases run while the runner's loop is
  not running, so an asyncio task would not tick through exactly the window that matters), writing
  every interval from the top of run(), handed over to the steady loop at _serve, and its observed
  reclaim raised before any strategy starts -- deviating from the story's
  created-after-the-trading-phase placement, flagged for the retro. Patches worth naming: a reclaim
  swallowed by gather(return_exceptions=True) at cancellation is now folded into the ownership flag
  (join_heartbeat); `start` renders first-party failure text (pydantic spec errors, Postgres-layer
  errors) instead of withholding it as "the check"; the record constructor moved inside the AC #10
  guard window; asyncio.CancelledError joined the except tuples; the teardown-order test now
  injects each typed failure at its own phase (GateRefusedError previously had no order assertion
  at all); the spec-order test uses two strategies (vacuous with one); the placeholder no-call test
  records every node access via a proxy. Three deferred to deferred-work.md (unbounded teardown
  join on a wedged DB, clean-run release failure still exits 0, two timing-raced tests). Final:
  unit 2063 (+13), component 1221/16 skipped (+21), integration/core --forked 73/2 skipped,
  format/lint/typecheck clean, every file back under its size limit (runner 498).
  Previously: 2026-08-21 (Story 2.5 implemented -> review. ready-for-dev -> in-progress -> review. epic-2 stays
  in-progress: 2.1/2.2/2.3/2.4 done, 2.5 review, 2.6-2.8 backlog. Delivers `ntrader live start`: the
  eight-phase AR39 startup sequence, AR32's ~30s heartbeat writer, AR32's record port plus its
  SQLAlchemy adapter, the inert SessionController, and explicit logging/controller/six-timeout node
  configuration. All 11 tasks and every subtask complete; all 10 ACs satisfied.
  A REAL PAPER GATEWAY WAS LISTENING, so the command was run for real rather than only against
  doubles -- and this is the first complete connect -> gate -> subscribe -> trading round trip the
  phase has observed, which Epic 1's retro records as never having happened. Evidence: all sixteen
  phase records in PHASE_SEQUENCE order; gate:account verifying a masked ***626 paper account;
  strategies started; session.started carrying trader_id=PAPER-c697f850; 181 seconds of
  last_heartbeat_at advance (~6 beats), so AR32's producer is live and not merely tested; a Redis
  namespace of trader-PAPER-c697f850:* with NO instance-id segment, confirming Story 2.4's
  use_instance_id=False end to end on a real node; the row left `stopped`, not `running`, so the
  teardown ran on SIGTERM; and NO ORDER PLACED -- the three cached order events are all
  strategy_id=EXTERNAL and flagged reconciliation, a pre-existing 4-share AAPL position Nautilus
  discovered in the paper account. This is NOT a P6 pass: that procedure needs 6.5 hours and is
  written and recorded as not run, because AC #7's automated tests are proxies and the story says so.
  SEVEN MUTATIONS, SEVEN KILLS. Three are encoded as tests that re-run every build, including a
  real-`Trader` integration test proving `add_strategy` registers ZERO strategies without a
  controller (with one: one) -- the single fact the whole zero-strategies-then-add design rests on,
  and one a TestLiveNode double cannot prove because it does not implement the guard.
  FOUR DEVIATIONS, all in the completion notes. (1) SessionService was at 97 of the 100-line class
  limit, so `transition()`'s body moved to a module-level `_apply_transition` alongside the new
  `_stamp_activity` -- AR37 is enforced per FILE by an AST scan and a scoped grep, so
  session_service.py is still the only module that assigns status and all of Story 2.3's tests pass
  UNMODIFIED; the class is now 99. (2) The runner needed a SECOND module split beyond the
  pre-agreed live_session_steady_state.py: live_session_node.py, mirroring live_check_node's
  relationship to live_check_driver. The phase sequence and the `finally` stayed whole, as Story 2.6
  requires. Final sizes: runner 496, node 248, steady state 258, live_node_builder 496, live.py 468
  -- all under 500, several uncomfortably close, and recorded as a budget constraint on 2.6. (3) The
  record port WIDENS the story's literal rule: any InvalidSessionTransition from record_activity is
  treated as loss of ownership, not only the last_started_at reclaim, because a row that became
  `stopped` out of band means the same thing to a runner and the alternative logs an error every 30s
  forever against a row that will never accept another write. The runner cannot catch the DB
  exception at all (AR38), so a pure SessionReclaimedError lives in the port's module and the
  adapter translates. (4) SessionReclaimedError is a FOURTH name in _SAFE_MESSAGE_EXCEPTION_NAMES
  where only three go into _OUTCOME_BY_EXCEPTION_NAME -- the two answer different questions, and
  Judgment call #8 says a fourth typed failure is the moment to revisit the marker protocol.
  ONE TEST COULD NOT FAIL and was caught before review: the contextvars assertion passed standalone
  and silently observed NOTHING under -n auto, because this repo sets
  cache_logger_on_first_use=True and an earlier test in the worker had already bound live_check's
  module logger against the old processor chain. Fixed by rebinding a fresh UNBOUND logger after
  reconfiguring (which preserves the test's meaning -- contextvars is still the only route a
  session_id can take) plus a permanent negative control asserting the same capture yields no
  session_id without the binding. deferred-work.md: nine items STRUCK (the gate-placement blocker and
  the heartbeat-writer blocker both close, the latter only in half -- there is still no fencing
  token), five re-annotated, and a new story-2.5 section. Final: unit 2050 (+100), component 1200
  passed / 16 skipped (+123), integration/core --forked 73 passed / 2 skipped, integration/db 70,
  e2e+api+ui 193, zero regressions; format/lint/typecheck (100 files) clean; 40/40 Epic 1 acceptance
  criteria still pass. NOTE: the smoke test left a real trading_sessions row, smoke-1787319225
  (c697f850-...), now `stopped` -- the repositories are write-once and expose no delete, so it is
  named here rather than removed with raw SQL.
  Previously: 2026-08-19 (Story 2.5 created -> ready-for-dev. epic-2 stays in-progress: 2.1/2.2/2.3/2.4 done,
  2.5 ready-for-dev, 2.6-2.8 backlog. The story was drafted from a 10-agent parallel artifact sweep
  plus direct verification against the installed nautilus-trader 1.220.0, and it settles the one
  finding the Epic 1 retrospective flagged as a blocking design constraint: NautilusKernel's
  start_async() offers no hook between "engines connected" and "trader started", so AR39's
  gate:account phase cannot be placed by polling exec_engine.check_connected(). The design takes the
  third mechanism the retro named -- build the node with ZERO strategies and no declaratively
  attached bar observer, run gate:account, then register and start the observer (subscribe phase)
  and the strategies (trading phase). That requires a Controller, because Trader.add_strategy and
  add_actor SILENTLY no-op on a running trader unless has_controller is set (trader.py:395-397,
  kernel.py:480) -- a failure mode that would have produced a session trading nothing with no
  exception and one ERROR log line. Verified in the wheel: Component.start() runs START (transitory,
  action=_start) then START_COMPLETED, so trader.is_running is a genuine post-condition of
  Trader._start() and is the correct connect-completion signal, where check_connected() turns True
  partway through a single coroutine and races it. Three ACs were added beyond the epic's seven, all
  traceable to Epic 1 retro Action Items 4-6 and 10: structural (not racy) gate:account placement
  plus a runner-owned has-started fact; explicit LoggingConfig/timeouts/controller on
  TradingNodeConfig asserted by name; and a bounded-failure AC covering exit codes, the teardown-
  before-stopped ordering, and never retrying a BacktestStorageError. Two blocking deferred items
  close here (the heartbeat writer with no producer, and the gate-placement blocker); four fragmented
  no-bars-watchdog entries close together as visibility-only, with the broker-authoritative fix left
  to Epic 4. Measured during drafting and folded into the story: LiveClock timer callbacks run on a
  foreign Rust thread with empty contextvars, silently swallow exceptions and fire catch-up bursts,
  so the ~30s heartbeat is an asyncio task bridged with asyncio.to_thread, not a Clock timer; and
  structlog.testing.capture_logs strips contextvars, so the runner binds session_id BOTH via a bound
  logger (testable) and via bind_contextvars (blanket coverage). Eight judgment calls flagged for the
  Epic 2 retro, the two largest being that the runner owns an explicit event loop rather than
  asyncio.run -- architecture.md:311 says otherwise, but TradingNode.dispose() calls loop.stop() and
  Story 1.3 lost a live run to exactly that -- and that last_bar_at is observed per bar but persisted
  on the heartbeat tick, because a per-bar DB round trip on the event-loop thread is the accumulating
  backlog NFR2 forbids in the same story that has NFR2 as an AC. Previously: Story 2.4 code review
  -> done. review -> done. epic-2 stays in-progress: 2.1/2.2/2.3/2.4
  done, 2.5-2.8 backlog. Three adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance
  Auditor) over a8fa04a, every finding then put to an independent verifier told to refute it against
  running code: 33 raised, 20 refuted as noise, 13 survived. 3 needed a human call, all resolved by
  the operator; 11 patches applied, 2 deferred. TWO REAL DEFECTS, both reproduced by execution
  rather than argued. (1) check_redis_reachable REPORTED SUCCESS for any endpoint that completed a
  TCP handshake without answering +PONG -- against a listener that accepts and never speaks, the
  preflight passed and CacheDatabaseAdapter was still blocked, silently, when killed at 40s: the
  exact hang AC #6 exists to bound, reinstated by the two warn-and-return branches. Both now raise.
  The module docstring had argued the opposite ("refusing because a proxy answered unusually would be
  a worse failure"); that premise was measured false and the docstring rewritten. Cost accepted: a
  requirepass Redis is now refused here rather than failing fast inside the adapter with NOAUTH.
  (2) scripts/diagnostics/live_connection_probe.py DIED AT IMPORT -- Story 2.4's extraction moved
  read_ibkr_connection_status and this caller was missed, so the tool docs/qa/phase3-live-verification
  .md names for the NFR32/NFR34 reconnect verification was unrunnable. Completion Note #2's "no
  production module imported it (verified by grep)" was wrong. Repointed, and renamed to
  live_connection_loss_probe.py to clear the basename collision with src/core/live_connection_probe.py
  (NOT live_reconnect_probe.py -- ibkr_reconnect_probe.py already owns that name). ALSO: CI had no
  redis service, so the four integration tests proving AC #4/FR19 -- the story's central guarantee --
  always skipped in CI and could have broken with every build green; redis:7-alpine added to the
  integration-tests AND coverage-report jobs. A test that claimed to prove config assembly never
  probes Redis patched the wrong module and could never fire. Nothing pinned the check_redis_reachable
  signature deviation's whole point (preflight reads cache.database.*, not settings); that pin now
  exists and was mutation-checked. The 1000-draw distinctness test was a ~1-in-8,600 flake and is now
  seeded; the 8-hex collision bound was stated an order of magnitude low (10**7 -> 1.2 in 10**6 at 100
  sessions) in both live_trader_id.py and deferred-work.md. README gained the three REDIS_* rows.
  Deferred: REDIS_DB=1 takes down every CLI entry point (pre-existing module-scope get_settings blast
  radius, deferred-work.md:126-133) and src/config.py at 493/500 lines. Gates re-run clean: format,
  lint, mypy 93 files, 1950 unit, 1061 component, 68 integration-core --forked, 40/40 Epic 1 ACs.
  Previously: 2026-08-19 (Story 2.4 implemented -> review. ready-for-dev -> in-progress -> review, same session
  as creation. epic-2 stays in-progress: 2.1/2.2/2.3 done, 2.4 review, 2.5-2.8 backlog. Delivered
  TDD Red->Green: RedisSettings (host/port/db, refusing a non-zero db rather than ignoring it),
  src/core/live_trader_id.py (deterministic PAPER-<8 hex>, framework-free), src/core/live_cache.py
  (CacheConfig with the three namespace-deciding fields pinned explicitly + a stdlib-socket
  reachability preflight), and a cache= seam on both live_node_builder entry points with the
  preflight strictly before TradingNode(...). 35 new tests; 1950 unit / 1055 component / 68
  integration-core / 115 e2e+api all pass; mypy clean; 40/40 Epic 1 acceptance criteria still pass.
  THREE DEVIATIONS FROM THE STORY, all in the completion notes. (1) check_redis_reachable takes
  host/port rather than RedisSettings -- build_trading_node holds IBKRSettings, and more importantly
  a settings-shaped preflight can check a different Redis than the node connects to; it now reads
  cache.database.host/port so the two cannot drift. (2) read_ibkr_connection_status was EXTRACTED to
  a new src/core/live_connection_probe.py: adding the cache seam took live_node_builder.py to 515
  lines against the repo's 500-line cap -- the exact risk the story's Judgment call #1 flagged. Pure
  move, no behaviour change, no production importer (only its own suite, already named for it);
  builder now 416 lines. A Story 1.6 surface touched by a 2.4 task -- flag at review if unwanted.
  (3) The existing Epic 1 guard test_no_new_dependency_was_added_for_the_live_path correctly failed
  on the new socket import; socket was added to its hand-curated stdlib allowlist WITH the reason
  inline rather than switching the guard to sys.stdlib_module_names, because an auto-derived
  allowlist would wave future imports through silently. Mutation testing per Epic 1 retro Action
  Item #3 was done twice: .hex->str() in derive_trader_id failed 5 tests as designed, and the
  use_instance_id proof is encoded as a permanent integration test rather than a one-off procedure.
  Two live-Redis discoveries for Epics 3/4: cache writes are ASYNCHRONOUS (add() then keys() on the
  same open adapter returns []; close() is what makes a write readable) and
  CacheDatabaseAdapter.flush() is FLUSHDB, clearing the whole database rather than the trader
  namespace -- so the tests derive trader ids from a fresh uuid4() instead of flushing. Story 2.2's
  deferred model_copy item, pointed at this story, is resolved as NOT-TRIGGERED and re-pointed at
  Story 2.5. NOT DONE deliberately: .env.example gained no REDIS_HOST/REDIS_PORT -- the file is
  hook-protected and needs the one-off approval Story 1.2 used; defaults are correct without it, so
  it is a documentation gap only, recorded in deferred-work.md.
  Previously: 2026-08-19 (Story 2.4 created: backlog -> ready-for-dev. epic-2 stays in-progress:
  2.1/2.2/2.3 done, 2.4 ready-for-dev, 2.5-2.8 backlog. File:
  2-4-give-each-session-its-own-durable-engine-cache.md. Delivers the session-derived Nautilus
  trader_id and the Redis-backed engine cache -- the phase's one hard cross-epic dependency, since
  AR23's session-stable client order IDs are what E3 needs from it. Drafting executed the installed
  nautilus-trader 1.220.0 rather than reading its docs, and four findings changed the story's shape.
  (1) CacheDatabaseAdapter.__init__ HANGS FOREVER against an unreachable Redis -- exit 124 after 45s,
  no exception, and DatabaseConfig(timeout=2) does not bound it. NautilusKernel builds that adapter
  eagerly, so TradingNode(config=...) with Redis down would produce a silent unkillable-looking
  start. Added AC #6 (a bounded stdlib-socket preflight before node construction); it is Epic 1 retro
  Action Item #10's defect one layer down. (2) The whole of "a restarted process rejoins its own
  state" rests on two silent third-party CacheConfig defaults, use_instance_id=False and
  flush_on_start=False -- either flipping makes AC #4 false with no error anywhere. Added AC #7
  (pin all three namespace-deciding fields explicitly, assert each by name), the same treatment
  live_node_builder.py:235-241 already gives market_data_type. (3) TraderId.get_tag() splits on the
  LAST hyphen and ClientOrderIdGenerator embeds that tag, so the short session id must use uuid.hex,
  never str(uuid) -- a str() slip yields a VALID TraderId with a wrong 12-char tag and raises
  nothing. (4) DatabaseConfig has no db/database-index field at 1.220.0, so AR11's REDIS_DB has
  nowhere to go; the field ships but a non-zero value is refused rather than silently ignored.
  Two structural constraints also fixed the file layout: live_node_builder.py is 493 lines against
  the repo's 500-line cap, forcing the cache into its own module, and AR38's Nautilus-free rule for
  the service layer forces the trader_id derivation into a second, framework-free one
  (src/core/live_trader_id.py + src/core/live_cache.py). BLOCKERS the dev must clear first: Redis is
  not running here (6379 closed, Docker daemon down, no brew redis) and Task 5 cannot be written
  without it; .env.example is hook-protected, so any REDIS_HOST line there needs the same one-off
  approval Story 1.2 used. Deferred-work item "model_copy bypasses validation", which Story 2.2
  pointed at this story, is resolved as NOT-TRIGGERED here and re-pointed at Story 2.5.
  Previously: 2026-08-19 (Story 2.3 code-reviewed -> done, closing it. epic-2 remains in-progress: 2.1 done,
  2.2 done, 2.3 done, 2.4-2.8 backlog. Three adversarial layers produced 42 raw findings, 26 after
  dedupe: 3 decisions / 15 patches / 7 deferred / 4 dismissed. ONE CRITICAL DEFECT SURVIVED THE
  STORY'S OWN MUTATION TESTING and was found here: transition()'s SELECT ... FOR UPDATE took the row
  lock correctly but SQLAlchemy returned the identity-mapped instance and DISCARDED the freshly
  locked row's column values, because no populate_existing was set anywhere in src/. A caller that
  had already resolve()d the row -- the shape AR36 prescribes -- therefore validated and decided on
  PRE-LOCK state. Reproduced twice against the live database: two processes both reclaimed one
  session (AC #6 violated, the outcome the story calls the worst this phase can produce), and a
  SEALED session was moved back to RUNNING (AC #3's terminality violated). Neither the unit tier
  (repository fully mocked, no identity map) nor the shipped race test could catch it -- the race
  test's loser uses a brand-new Session whose identity map has never seen the row, the one
  arrangement in which the ORM does return fresh data. Closed with
  .execution_options(populate_existing=True) on both repository twins (AR9) plus an integration
  regression test that reproduces the resolve()-then-transition() shape and goes red without the fix.
  Second-most-serious find: SessionStatus is a StrEnum, so `"running" in frozenset({RUNNING})` is
  True while `is` is False -- a raw-string target skipped the reclaim guard ENTIRELY rather than
  being evaluated by it; closed by normalising `to` through _as_status() before any decision.
  Allay ruled on all three decisions: (1) widen the AR37 AST guard -- it examined 4 of 192 files and
  a planted probe using the src.db.models re-export passed it, with src/cli/commands/live.py never
  scanned; now scans all of src/ with a 2-file allowlist and a non-vacuity assertion; (2) keep
  InvalidSessionTransition's BacktestStorageError base, with the never-retry constraint recorded for
  Stories 2.5/2.6; (3) accept that the heartbeat has no producer until Story 2.5, recorded as a
  BLOCKING constraint on that story -- until AR32's writer exists, every session is reclaimable 90s
  after it starts and the AC #5 refusal is inert. All 7 new/changed guards were mutation-tested and
  every one went red as predicted. AC #6 was reviewed on its merits, as the story invited, and KEPT:
  it is correct and it caught a real defect. Final: unit 1919 passed (+18), component 1031 passed /
  16 skipped, integration/db 70 passed, Epic 1 sweep 40/40. format/lint/typecheck clean.
  Previously: 2026-08-19 (Story 2.3 implemented -> review. ready-for-dev -> in-progress -> review, same session
  as drafting. Delivers SessionService.transition() and .resolve() in src/services/session_service.py
  (227 lines, class 88 — under the story's own tightest limit): the single validated path through
  which a trading session's status changes (AR37) and the shared name-or-UUID resolver (AR36). All 9
  tasks complete, all 13 ACs satisfied including AC #6 (the concurrent-reclaim criterion the story
  itself added, not in epics.md), demonstrated live against real Postgres with a genuine
  two-connection race (winner holds its FOR UPDATE lock open via a deliberate 0.4s delay so the loser
  actually blocks, then re-reads the heartbeat the winner just stamped and refuses) rather than
  simulated with mocks. Task 7's mutation testing confirmed all four load-bearing guards actually
  fail when broken: dropping with_for_update() failed the sync repository's structural guard; a
  throwaway status= assignment in a repository method failed the AR37 AST scan, naming that file;
  loosening the staleness comparison from > to >= failed the exactly-at-threshold boundary test; and
  removing the reclaim's last_heartbeat_at stamp failed the unit-tier property test pinning it (the
  story names an integration test for this mutation that had not been written yet at that point in
  the sequence -- the equivalent unit-tier test already covers the same fact, and the integration
  race test exercises it again under real concurrency once built). One self-inflicted trap caught
  during Task 6: session_service.py's own module docstring originally said "no nautilus_trader" and
  "never calls commit() or rollback()", which would have tripped the very grep gates Task 6 requires
  to return zero hits -- the same class of trap Story 2.1's review found in from_overrides'
  docstring; reworded before writing the guard tests. deferred-work.md gained a "Deferred from:
  story-2.3" section: closes nothing outright, decides (but does not close) the SessionStatus
  terminal-failure item from Judgment call #4 -- not a fifth status value, since the enum and its PG
  type are both pinned at four and this phase's single migration is spent -- re-pointed at Story 2.8;
  and reads three other stories' still-open items without touching them (re-validate-on-read ->
  Epic 5, model_copy bypass -> Story 2.4, repository CI-gating coverage -> Epic 2 retro), plus flags
  that InvalidSessionTransition is a new typed failure live_check.classify_failure will need mapping
  for a future CLI story (2.5/2.6, exit code 1). Final counts: baseline (collected, matching the
  story's own recorded figures exactly) unit 1856 / component 1047 / integration 239; final (passed)
  unit 1901 (+45) / component 1031 passed + 16 skipped (unchanged) / integration 240 passed + 2
  skipped (+3) -- zero regressions. format/lint/typecheck clean (session_service.py is inside
  make typecheck's src/services scope). Re-ran the FULL Epic 1 acceptance sweep across all six
  test_epic1_ac_*.py files (the story names only test_epic1_ac_node.py, which alone covers just 8 of
  the 40 criteria) -- 40/40 still pass, untouched by this story. epic-2 remains in-progress: 2.1/2.2
  done, 2.3 review, 2.4-2.8 backlog.
  Previously: 2026-08-19 (Story 2.3 created: backlog -> ready-for-dev. epic-2 stays in-progress: 2.1/2.2 done,
  2.3 ready-for-dev, 2.4-2.8 backlog. File:
  2-3-move-a-session-between-states-through-one-guarded-path.md. Owns SessionService.transition()
  (the single validated state machine), InvalidSessionTransition, a stale-heartbeat reclaim, and the
  shared name-or-UUID resolver. NO migration, NO CLI, NO runner — every column it writes already
  exists from 2.2's migration (re-verified against the live DB: head d08dfbd393f0 single, all five
  lifecycle timestamps timestamptz/nullable, pg_enum labels lowercase, trading_sessions 0 rows /
  backtest_runs 133 / trades 14982). Its context was built by executing against the live database
  rather than reading docs, which settled six things the epic could not. (1) THE RECLAIM IS A RACE,
  demonstrated both ways: two threads reading the same stale heartbeat with no row lock BOTH
  reclaim ("two processes on one session", the worst outcome this phase can produce); with
  .with_for_update() on the read AND the reclaim stamping last_heartbeat_at, the loser blocks 0.46s,
  re-reads a 0.5s-old heartbeat and is refused. Neither half works alone — drop the stamp and the
  loser re-reads a still-stale value and reclaims anyway. This is added as AC #6, which is NOT in
  epics.md, and flagged as such. (2) AC #1's grep is unsatisfiable as written: `grep -rn "status = "
  src/` matches 20 pre-existing lines and `\.status = ` matches 5, none touching trading_sessions —
  same wall Story 2.2 hit. Replaced with two scoped greps (both verified at 0 hits today) plus an
  AST scan restricted to modules that import TradingSession, which finds 0 offenders across exactly
  3 importers and cannot false-positive on the Pydantic self.status= lines in catalog_metadata.py /
  strategy.py. (3) psycopg2 returns timestamptz in the DB SESSION timezone (America/Toronto,
  -04:00), NOT UTC — a test asserting tzinfo is timezone.utc on a read-back value FAILS, and
  datetime.utcnow() minus that value raises TypeError. (4) last_heartbeat_at is nullable and
  `now - None` raises; NULL must count as STALE, matching ConnectionMonitor's own `age is None ->
  stale` rule, because the opposite reading manufactures exactly the stuck state NFR11 forbids.
  (5) TimestampMixin gives created_at ONLY — no updated_at, no onupdate — so every lifecycle
  timestamp must be written explicitly; the story supplies the edge->column map the epic never
  wrote, and flags that Story 5.3 must NOT also stamp sealed_at. (6) An unattached TradingSession
  built without status= has status is None (the ORM default applies at flush), a direct trap for the
  mocked unit tests. The load-bearing design call: the status assignment lives in
  SessionService.transition(), NOT in a new repository method — which satisfies AR37 literally,
  leaves both repositories' capability allowlist and the 2.2 shape guards passing UNMODIFIED (the
  repos gain only a for_update kwarg, invisible to dir()), and is a strictly stronger guarantee,
  since no repository method can write status at all. Seven judgment calls flagged for the Epic 2
  retro, the largest being the heartbeat staleness threshold — no number exists anywhere in the PRD,
  architecture or epics, only AR32's ~30s write cadence — set to 90s expressed as 3x the interval
  constant so Stories 2.5 and 2.8 import one value instead of inventing three. The deferred item
  routed to this story by name (SessionStatus has no terminal failure state) is DECIDED, not closed:
  not a fifth status value, because 2.1's AC pins four, the pg enum has four labels, and this
  phase's single migration is spent — re-pointed at Story 2.8 with that reasoning recorded.
  Previously: 2026-08-19 (Story 2.2 code-reviewed -> done, closing it. epic-2 remains in-progress: 2.1 done,
  2.2 done, 2.3-2.8 backlog. Three adversarial layers produced 40 raw findings (38 after dedupe);
  10 independent skeptics with repo access refuted 24 and confirmed 14, merging to 11 distinct
  items, plus 2 the reviewer found directly. NO critical, high or medium finding survived
  verification — the 63% refutation rate is the headline: several confident findings (a downgrade
  "silently destroying the system of record", a spec that is "not actually frozen", an ORM read
  after session close) collapsed on contact with the real code, because expire_on_commit=False is
  set globally, the downgrade behaviour IS AC #4, and the re-validation cost was already a tracked
  deferral. AC #1/#2/#3 were re-verified against the live database rather than taken on the
  story's word: status is a genuine session_status enum with lowercase pg_enum labels, all 13
  columns are timestamptz/jsonb as specified, uniqueness is unique INDEXES whose names match the
  ORM exactly (the drift the story feared did not happen), chk_trades_owner and
  trades_session_id_fkey (NO ACTION) are present, and 133 backtest_runs / 14982 trades are
  unchanged with 0 NULL backtest_run_id and 0 non-'backtest' run_type. Allay resolved the single
  decision (--name: reject blank and >100 chars at exit 2, no trimming — a frozen spec should
  store exactly what was typed) and all 12 patches were applied TDD Red->Green. The most valuable
  class of finding was tests that could not fail: the migration's upgrade-widening assertion was
  satisfied by nine unrelated `nullable=True` occurrences, so inverting AC #3's core clause left
  CI green; nothing at all guarded downgrade() restoring backtest_run_id to NOT NULL; and the
  escape() test asserted only an exit code, which rich never fails. All three were hardened and
  MUTATION-TESTED to prove they now go red. A fourth guard the reviewer wrote was itself caught
  by that mutation pass and rewritten as a structural AST assertion. Production fixes:
  from_stored now refuses a non-integer/bool schema_version with ValueError instead of a bare
  TypeError; schema_version defaults to SPEC_SCHEMA_VERSION so the writer and the read gate cannot
  drift; ORDER BY on both find_all()s ahead of Story 2.8's list; blank --param key rejected and
  unknown keys rendered with !r; compare_to annotated. One regression caught and fixed: adding
  `from uuid import UUID` to live.py broke Epic 1's dependency guard, closed by adding uuid to
  that test's STDLIB set rather than to PERMITTED_THIRD_PARTY. Final: unit 1831 -> 1856 (+25),
  component unchanged, integration 238 -> 237 (3 shape guards moved to the CI-visible unit tier,
  2 ordering tests added). 3 items deferred, 24 dismissed. Previously: Story 2.2 implemented ->
  review. ready-for-dev -> in-progress -> review, same
  session as starting. All 9 tasks complete, TDD Red confirmed before every implementation
  subtask, all 13 ACs satisfied. Delivers the phase's single Alembic migration
  (trading_sessions + backtest_runs.run_type + trades widened with chk_trades_owner), the
  TradingSession ORM model, both trading-session repositories, SessionSpec.to_stored()/
  from_stored(), and `ntrader live create`. Baseline confirmed against the live database before
  any edit: alembic head a436f35f525c (single head), 133 backtest_runs, 14982 trades — matching
  the story's own recorded figures exactly. The migration was hand-written (never
  --autogenerate) and round-tripped upgrade -> downgrade -1 -> upgrade against the live
  database; post-upgrade counts confirmed identical (133 / 14982), zero
  trades.backtest_run_id IS NULL, zero non-'backtest' run_type, and the session_status enum's
  pg_enum labels read exactly created/running/stopped/sealed. Both guards Task 9 calls
  load-bearing were mutation-tested per the Epic 1 retro's Action Item #3 and confirmed: deleting
  values_callable flips the enum to uppercase member names and fails the ORM-shape test;
  reverting SessionSpec.to_stored() to a bare model_dump() fails the unit json.dumps test with a
  bare TypeError and the integration JSONB round-trip test with sqlalchemy.exc.StatementError
  wrapping that same TypeError, exactly as the story predicted — both reverted immediately after.
  One pre-existing Epic 1 acceptance test needed a deliberate, scoped widening:
  test_no_new_dependency_was_added_for_the_live_path asserted live.py's third-party import
  surface against an allowlist scoped to Epic 1's check-only command; `create` legitimately
  imports pydantic and sqlalchemy, both already-declared project dependencies
  (pyproject.toml/uv.lock confirmed byte-identical via git diff --stat), not new ones — widened
  the allowlist with an explanatory comment; re-ran 40/40 Epic 1 acceptance criteria still
  passing. Two of Task 9's grep checks needed the same "pre-existing out-of-scope hit doesn't
  count" reading the story itself applies to the status = check: the model_dump( grep matches an
  unrelated pre-existing hit in the ValidatedJSONB type the Dev Notes say not to use (zero hits
  scoped to this story's own files), and the model_dump(mode="json") grep returns 2 hits not
  exactly 1 because Story 2.1's pre-existing module docstring already used the phrase once before
  this story added the method — reworded this story's own method-docstring mention rather than
  touch Story 2.1's untouched prose. deferred-work.md gained a new "Deferred from: story-2.2"
  section carrying forward three items: src/models/trade.py's Pydantic widening (owed to Epic 3
  Story 3.6), the model_copy re-validation item (re-pointed at Story 2.4, whose durable engine
  cache is the first plausible second read path), and the resolve_live_bar_types lowercase item
  (re-verified still open; this story's CLI is not exposed to it since --bar-type values route
  through StrategySpec's own canonicalisation). Final counts: baseline (collected, pre-edit) unit
  1780 / component 1047 / integration 217; final (passed) unit 1831 (+51) / component 1031
  passed + 16 skipped (unchanged) / integration 238 passed + 2 skipped (+21) — zero regressions.
  format/lint/typecheck clean (make typecheck plus a standalone mypy run against all seven
  new/modified src/db, src/models and src/cli files). epic-2: 2.1 done, 2.2 done pending review,
  2.3-2.8 backlog.
  Previously: 2026-08-19 (Story 2.2 implementation started: ready-for-dev -> in-progress. Beginning TDD
  Red-Green-Refactor across the nine tasks: unit-tier migration/ORM guards, the TradingSession
  ORM model, widening Trade/BacktestRun, the hand-written migration, both repositories,
  SessionSpec.to_stored()/from_stored(), and `ntrader live create`. Baseline confirmed against
  the live database before any edit: alembic head a436f35f525c (single head), 133 backtest_runs,
  14982 trades — matching the story's own recorded figures exactly.
  Previously: 2026-08-18 (Story 2.2 created -> ready-for-dev. Owns the phase's SINGLE Alembic migration
  (trading_sessions + backtest_runs.run_type + trades.session_id/nullable backtest_run_id + CHECK),
  the TradingSession ORM row, both repositories, and `ntrader live create`. Its context was built by
  executing against the installed libraries and the LIVE database rather than reading docs, which
  settled five things the epic could not: (1) sa.Enum(SessionStatus) persists the member NAME, so
  the DB labels would be CREATED/RUNNING/STOPPED/SEALED — values_callable is MANDATORY, and this is
  the first place in the repo that needs it, because the only existing DB enum (ResolutionStatus)
  deliberately uses name == value to dodge exactly this footgun; verified end-to-end against
  Postgres that values_callable yields the four lowercase labels architecture.md:380 requires.
  (2) spec.model_dump() raises TypeError: Object of type Decimal is not JSON serializable when bound
  to a real JSONB column, while model_dump(mode="json") round-trips losslessly and restores Decimal
  on the way back in — verified by inserting both into a temporary JSONB table. (3) ValidatedJSONB,
  the type a dev agent would naturally reach for, is wired to NO column anywhere, has impl = JSON
  rather than JSONB, and its python-mode process_bind_param reproduces that same TypeError — it must
  not be used; BacktestRun.config_snapshot is plain JSONB. (4) A typo'd --param key is SILENTLY
  dropped by StrategyLoader.build_strategy_params (verified: overrides={"fast_perios": 12} yields
  fast_period=10 with no error anywhere), and FR14 then freezes that wrong value for the session's
  whole life — so the CLI must validate keys against param_model.model_fields, which closes the item
  deferred from story-2.1. (5) String --param values coerce correctly through the chain (int and
  Decimal both) and failures surface as a plain ValueError, not a pydantic ValidationError, so the
  CLI's catch tuple must be (ValidationError, ValueError). Also recorded: autogenerate is UNSAFE for
  this migration because two backtest_runs indexes exist in the ORM but in no migration; the entire
  tests/integration/db/ directory is --ignore'd in CI, so the migration guard must live in the unit
  tier; no alembic downgrade test has ever existed in this repo; and the live DB is at head
  a436f35f525c with 133 backtest_runs and 14982 trades, all of which satisfy the new CHECK
  constraint. Four deferred-work items from the story-2.1 review are addressed here (override-key
  validation, schema_version read gate, model_dump persistence rule, the CLI catch tuple); the
  model_copy and re-validate-on-read items stay deferred with reasons. Seven judgment calls are
  flagged for the Epic 2 retro, the largest being one strategy per `live create` invocation (the
  list shape lives in the model, where FR13 lives) and SyncTradingSessionRepository over the
  architecture doc's TradingSessionRepositorySync, since all five existing sync repositories use the
  Sync-prefix form (recorded on AC #8 as a knowingly-unmet epic AC, not a miss). The draft was then
  validated in a fresh context against the create-story checklist: ~85 claims re-verified by
  execution and 4 real defects in the draft corrected before hand-off — the largest being that the
  "do not autogenerate" rationale was itself wrong (compare_metadata returns 0 diffs against the
  configured DB, and ff982c8e1402 repairs a nullability drift, not an index one; the real hazard is
  ORM-vs-migration-chain, since 34f3c8e99016:71 creates idx_trades_backtest_run_id where the ORM's
  index=True wants ix_trades_backtest_run_id), and that Task 9's `grep -rn "status = " src/db/` gate
  could never have passed because 6 pre-existing resolution_status assignments already match it.
  Previously: Story 2.1 code-reviewed -> done. Three adversarial layers (Blind Hunter, Edge Case
  Hunter, Acceptance Auditor); 53 raw findings -> 35 after dedup: 6 decisions, 17 patches, 11
  deferred, 5 dismissed. Every finding was re-verified by execution before triage, and two agent
  claims were refuted and dropped (the tuple-has-no-append tests ARE load-bearing — a list field
  makes them fail; and StrategyRegistry.get raises only KeyError on the miss path, with no alias
  shadowing a canonical name across all 28 entries). Allay ruled on all 6 decisions in one batch.
  The two load-bearing behaviour changes: (1) bar types are now stored in canonical UPPER-CASE by
  _resolve_bar_types, because BarType.from_str preserves instrument-id case and
  subscription_bar_types appended the caller's original string — so a lowercase entry won the
  cross-strategy dedup and reached load_ids as an id IBKR cannot resolve, which
  live_market_data.py:199-204 names in its own comment as "a silently dead subscription"; verified
  before (frozenset({'aapl.nasdaq'})) and after (frozenset({'AAPL.NASDAQ'})). Canonicalising follows
  the same store-canonical rule AC #2 already sets for strategy_id. (2) SessionSpec now refuses two
  entries naming one strategy — they share Nautilus' order_id_tag and Trader.add_strategy raises
  RuntimeError at node start; refusing at config time is reversible where the alternative
  (a per-entry instance tag on a frozen, persisted model) is a Story 2.5 guess. Six further code
  holes closed: a whitespace-only strategy_id constructed a fully unvalidated spec (no registry, no
  params, no bar types); _resolve_bar_types' list(raw) exploded a bare string into one bar type per
  character and destroyed the purpose-built message live_market_data.py:147-153 exists to give;
  non-sequence bar_types escaped as raw TypeError; extra="forbid" now rejects a mistyped model-level
  key that used to yield a silent all-defaults spec; "or {}" no longer swallows falsy non-dict
  parameters; and from_overrides resolves the identifier once through _lookup_strategy, fixing a
  fuzzy-id divergence (sma-crossover worked via the constructor, failed via from_overrides) and
  pre-checking param_model is None so its remediation message is reachable instead of an
  AttributeError from strategy_factory.py:344. Seven test-strength gaps closed, each of which
  previously could not fail: bar-type refusals now assert the resolver's own message; the round trip
  asserts byte-identical JSON (Decimal("10.0") == Decimal("10") made the old check blind to exponent
  loss); the credential test uses a sentinel instead of live env behind "if value:" (vacuous in CI)
  and adds username plus a parameters-key scan; import purity is measured in a subprocess (the AST
  proxy passes for a src.* import that transitively loads nautilus); the _settings_map link is now
  falsifiable via injected settings that differ from the param-model defaults (the repo's real
  settings equal them, so the old assertion passed even if that step were deleted); AC #3's
  never-calls-get_settings clause is a test rather than a one-off grep; and param_model is None has
  coverage. Docs corrected in the safety-relevant direction per the Epic 1 retro: the module
  docstring now discloses four limits (in-place parameters mutation, model_copy(update=) bypassing
  validation, specs not being hashable, schema_version recorded-not-enforced) each pinned by a test,
  and no longer states a Story 2.2 guarantee in the present tense. Baseline 1743 -> 1780 passed
  (+37), zero regressions; format/lint clean; import purity re-confirmed ([] leaked). Deferred: 11
  items in deferred-work.md, notably the pre-existing root of the casing bug —
  resolve_live_bar_types itself returns the un-canonicalised BarType, so a single-strategy lowercase
  entry still yields a lowercase load_id for every other caller. Previously: 2026-08-17 (Story 2.1
  implemented -> review. ready-for-dev -> in-progress -> review, same session as
  drafting. Built exactly to the Dev Notes' pre-verified design with no deviation: src/models/session.py
  (SessionSpec, StrategySpec, SessionStatus, three lazy-import helpers) plus
  tests/unit/models/test_session_spec.py (25 tests, one per AC-level behaviour). TDD Red confirmed
  first (ModuleNotFoundError before the module existed), then Green on the first implementation
  attempt — no iteration needed. Baseline was 1718 unit tests collected before any edit; final count
  1743 passed (1718 + 25 new), zero regressions; format/lint clean; a standalone mypy run against the
  new file (outside make typecheck's src/core + src/services scope) reports no issues; import-purity
  confirmed both by the story's own AST-based test and by running
  `python -c "import sys, src.models; print('nautilus_trader' in sys.modules)"`, which prints False.
  The one load-bearing new guard (strategies: tuple, not list, defeating frozen=True's shallow
  mutability hole) was mutation-tested per the Epic 1 retro's Action Item #3: swapped to list[...],
  watched the append-guard test fail, reverted. One cosmetic fix made after Green: from_overrides'
  docstring originally said "never calls get_settings()", which tripped Task 4's own grep gate
  (grep -n "get_settings\|os.environ\|getenv" — it does not distinguish code from prose); reworded to
  clear the gate. Recorded the one item Dev Notes flagged in advance for deferral —
  StrategyLoader.build_strategy_params silently drops an override key that is not a param-model field,
  pre-existing and shared with the backtest path — as a new "Deferred from: story-2.1" section in
  deferred-work.md, for Story 2.2's CLI to decide whether to validate override keys before calling.
  Scope discipline held: no migration, no ORM model, no repositories, no SessionService, no trader_id
  derivation, no CLI, no runner — confirmed by git status showing only the five files this story
  touches. epic-2 remains in-progress: 2.1 done pending review, 2.2-2.8 backlog.
  Previously: 2026-08-17 (Epic 2 opened. Story 2.1 created: backlog -> ready-for-dev; epic-2: backlog ->
  in-progress. File: 2-1-define-a-session-as-a-list-of-strategy-specifications.md. Pure domain-model
  story — src/models/session.py (SessionSpec, StrategySpec, SessionStatus) plus
  tests/unit/models/test_session_spec.py, nothing else; no migration, no service, no CLI, no runner.
  Five design findings were verified by execution against pydantic 2.11.9 and the live
  StrategyRegistry before the story was written, and are recorded in its Dev Notes rather than left
  to be rediscovered: (1) pydantic frozen=True is SHALLOW — a list field on a frozen model still
  accepts .append(), which would defeat FR14, so strategies/bar_types are tuples; (2) Decimal
  parameters do NOT survive a naive JSON round trip (Decimal -> "1000000" -> str), so AC #5's
  losslessness depends on re-coercing parameters through the strategy's registered param_model in a
  mode="before" validator; (3) a KeyError raised inside a pydantic validator ESCAPES rather than
  becoming a ValidationError, and both StrategyRegistry.get() (KeyError) and resolve_live_bar_types()
  (LiveMarketDataError) must be converted to ValueError; (4) resolve_live_bar_types correctly rejects
  duplicate bar types WITHIN one strategy but that is wrong ACROSS strategies, so SessionSpec grew a
  subscription_bar_types property that dedupes case-insensitively before anything reaches
  build_trading_node_config; (5) epics.md line 744 names StrategyFactory.build_strategy_params(),
  which does not exist — the real symbol is StrategyLoader.build_strategy_params() in the same
  module. Five judgment calls are flagged in the story for the Epic 2 retro (tuple-not-list,
  bar-type validation folded in, schema_version on SessionSpec, from_overrides classmethod,
  subscription_bar_types). AC #4 does NOT close the deferred-work item that flagged
  IBKRSettings.model_dump()'s credential leak: that item's trigger — "the moment Epic 2 persists
  session configuration" — has not arrived, because nothing persists anything until Story 2.2. What
  AC #4 establishes is narrower and forward-looking: the spec has no field those values could
  occupy, so when persistence does land it cannot leak them through this model. The item at
  deferred-work.md:38-43 remains open and untouched. Epic 1 retro action items 4-10 are Story 2.5
  constraints, not 2.1 ones, and travel forward.)

  2026-08-17 (Epic 1 retrospective completed — documentation-driven per Allay's direction: synthesized
  from deferred-work.md and all 7 stories' Dev Agent Records rather than a live interview. Full
  writeup at _bmad-output/implementation-artifacts/epic-1-retro-2026-08-17.md. Headline finding: the
  three-layer adversarial code review has a 7-for-7 hit rate this epic on catching a real, load-bearing
  safety bug the story's own TDD missed (e.g. 1.3's uncatchable Rust panic on a malformed trader_id,
  1.5's delayed-data guard that would have killed every healthy session at RTH re-open, 1.6's halt
  clock defeated by a flapping connection for 2.8 simulated hours) — recommendation is to keep it
  mandatory for every remaining epic. Two items multiple stories flagged as "action at the Epic 1
  retro" were resolved directly in epics.md rather than re-deferred again: AR20 (described a LogGuard
  registration ordering the Nautilus API cannot expose; reworded to "earliest point reachable from
  calling code") and AR41 (added connection.halted / connection.recovery_refused, shipped by Story 1.6
  but missing from the enumeration; recorded that command-scoped events like gate.static and
  live_check.* are deliberately outside this session-scoped list). One significant, evidenced
  discovery flagged for Epic 2's Story 2.5 — no AC rewrite needed, but a concrete implementation
  constraint to plan around rather than rediscover: NautilusKernel.start_async() has no hook between
  "engines connected" and "trader started", so gate:account cannot be placed by polling once a
  strategy exists (Epic 1 never hit this — it configures zero strategies). Readiness note: live
  verification is real but incomplete — of five procedures (P1-P5), only P1 fully passed; no procedure
  has ever observed a complete connect -> stream -> strategies-would-start round trip live, start to
  finish. epic-1-retrospective: optional -> done.

  This entry also closes a gap in this file's own history below, which never recorded Story 1.7's
  completion or Epic 1's closure. Story 1.7 implemented -> code-reviewed -> done, closing Epic 1 (all
  7 stories done, epic-1: in-progress -> done). Delivers `ntrader live check`: src/core/live_check.py
  (pure classification, unit-tier) and live_check_driver.py (the impure seam that builds, connects and
  observes via preflight_gate then build_trading_node, mapping outcomes to AR28's exit codes 0/1/3/4)
  plus the `ntrader live` CLI group. The scheduled code-review step was never reached on the first
  pass — that session died on an API drop before commit/review, leaving an uncommitted worktree; a
  2026-08-15 salvage session re-derived and re-verified everything rather than trust the recorded
  "Final" numbers, and a manual diff audit (not just a test re-run) surfaced two undocumented defects
  that had crept into the uncommitted state: an overly broad `except OSError` classification (would
  have misfiled a local log-directory permission error as BROKER_UNREACHABLE, exit 4), narrowed to the
  named exceptions that actually mean unreachable-broker; and a wrong import path in
  tests/component/core/test_live_check_driver.py that silently broke that file's collection entirely.
  A manual review substituted for the missed code-review step, concluding no further issues. The live
  runs found and fixed the story's most consequential bug: the first unreachable-gateway run took 115
  seconds to report failure because --connect-timeout's deadline and node.build()'s own
  IB_MAX_CONNECTION_ATTEMPTS retry budget were being spent in series rather than as one bounded window;
  fixed by starting the deadline before node.build() and dropping the build's own retry budget to 1
  attempt. Procedure P5 is recorded as "partial", not a pass: gate-refusal and broker-unreachable paths
  were verified live, but the full happy-path criterion was never observed — the available paper
  Gateway accepts a TCP connection but never completes the IB API handshake (error 502), reproduced
  with a fresh IBKR_LIVE_CLIENT_ID to rule out a stale-client-id cause. Final counts after the salvage:
  unit 1714 passed (one fewer than the pre-salvage 1715, since a stale bare-OSError test
  parametrization was removed), component 1020 passed / 16 skipped (matching the pre-salvage figure
  once the import-path break was fixed), integration 170 passed / 2 skipped / 0 failed, ruff/mypy
  clean. Previously: 2026-08-11 (Story 1.6 merged into the Epic 1 integration branch, joining Stories 1.4 and 1.5.
  Like 1.5, it was developed on a parallel branch and its artifacts were reconciled on merge: the
  sprint board now carries 1-4, 1-5 AND 1-6 as done, deferred-work.md carries all three stories'
  sections, and Story 1.6's live-verification procedure — drafted as "P2" while 1.4 and 1.5 were
  in flight, each drafting its own — was renumbered to Procedure P4 in
  docs/qa/phase3-live-verification.md, since 1.4's account-gate procedure holds P2 and 1.5's bars
  procedure holds P3. Any Story 1.6 artifact that still says "Procedure P2" means P4; the QA doc
  and deferred-work.md both record the renumbering inline. Two cross-story deferrals were
  reconciled at the same time and NEITHER is closed: Story 1.5 deferred "the observer is silent
  when no bar is delivered at all" TO Story 1.6, which turns out to detect a dead SOCKET rather
  than an absent BAR; and Story 1.6 deferred its IB-error-1101 item (1101 re-sets the socket flag
  with every subscription silently dropped) TO Story 1.5, which measures bar LATENESS rather than
  resubscription state. They are the same blind spot approached from two sides, and both now point
  at Epic 2's runner (an interim first-bar watchdog) or Epic 4 (broker-authoritative state).
  Story 1.6 implemented -> code-reviewed -> done. Delivers the connection state machine
  and the trading-permission flag: new pure `src/core/live_connection_monitor.py` (unit tier, no
  Nautilus) plus `read_ibkr_connection_status()` added to `live_node_builder.py`, where the
  adapter's `(host, port, client_id)` cache key cannot drift from the configured one. The finding
  that shaped the whole story: at nautilus-trader 1.220.0 an IBKR connection drop publishes NO
  message-bus event and changes NO public connection property -- the adapter's watchdog calls the
  `_degrade` *hook* directly rather than the `degrade()` FSM transition (so `is_degraded` stays
  False, verified by execution), and `is_connected` / `DataEngine.check_connected()` move only on
  the connect()/disconnect() lifecycle, reading True straight through a dead socket. Detection
  therefore polls the adapter's two private asyncio.Event flags, fail-closed, with a canary test
  that fails by name if either moves. Code review 2026-08-09 (3 adversarial layers, 34 raw findings
  -> 3 decisions / 17 patches / 3 deferred / 3 dismissed) was where this story earned its keep: all
  three decisions were ruled option (a) by Allay and each closed a distinct way `trading_permitted`
  could be True when it must not be, none of which the original 31 tests could reach. (1) The halt
  clock is now ANCHORED at the loss and cleared only by a successful confirmation, with the
  deadline evaluated after every observation rather than only in the LOST branch -- previously a
  gateway flapping up for one poll every 59s reset the 60s window indefinitely (200 cycles / 2.8h
  of an unusable link produced zero `connection.halted`), and an unconfirmed recovery sat in
  RECOVERING for a simulated 24h with no log line at all. (2) `trading_permitted` now EXPIRES when
  the last reading is older than `max_observation_age_seconds` -- the one fail-open shape no
  *observation* can express, a dead poll loop, which left permission granted forever through a dead
  socket. (3) `confirm_state_reestablished(status)` is a deliberate BREAKING signature change: the
  no-arg form granted from cached state, so a socket dying between the caller's last poll and its
  confirmation bought a whole poll interval of blind trading. Deliberate, tested consequence worth
  knowing: the halt measures UNAVAILABILITY (loss -> permission restored) while
  `within_reconnect_window` reports NFR4's narrower RECONNECT time, so a 2s reconnect followed by
  70s of reconciliation halts while still reporting the reconnect as fast. Also fixed: constructor
  validation (a `nan` window silently disabled the halt forever), clamped backward clock steps, a
  half-up first connection no longer emits `connection.lost` for a broker never reached, the
  reader's fail-closed guard widened to cover the attribute read and extended to reject a
  disposed/stopped client left in the never-purged IB_CLIENTS, and the probe hardened (build inside
  the try/finally, bounded build and join, waits on the SOCKET flag rather than certifying a
  cleared readiness flag as a genuine disconnect). Two findings were dismissed after verification
  rather than fixed: the client-ID-collision report is a false positive
  (`validate_client_ids_distinct` rejects it) and "a halt is escapable" is specified behaviour --
  both raised by the Blind Hunter, which by design has no spec or project access. AC #6 clean: no
  order path anywhere in the diff, pyproject.toml/uv.lock untouched, `src/core/live_gate.py`
  byte-identical -- the two-factor gate was neither weakened nor duplicated, and `trading_permitted`
  is a runtime availability flag, deliberately not a second gate (AR43). Final on its own branch:
  unit 1617 passed (1561 + 56), component 855 passed / 16 skipped (853 + 18), Story 1.3's 2
  integration tests unaffected, lint/format/typecheck clean; the merged integration branch's counts
  are the union of these and Story 1.5's. Procedure P4 is DEFINED but NOT RUN -- no IB Gateway was
  listening (all four IB ports refused TCP) and this worktree has no `.env` (gitignored, and
  hook-protected, so creating one was neither attempted nor appropriate); the probe was exercised
  as a dry run and behaved correctly on the fail-closed path, which this repo's QA policy counts as
  tooling evidence and explicitly NOT a pass. AC #1-#5 rest on 56 unit + 18 component tests that
  need no broker (NFR32/NFR34). Three items deferred under "Deferred from: code review of
  story-1.6": IB error 1101 can re-set the socket flag without any resubscription (needs a
  subscription-state signal Epic 1 does not own -- see the reconciliation note above),
  `live_node_probe.py` inherits the same build-outside-try hang, and AR41's normative event list
  still omits `connection.halted` and `connection.recovery_refused`. Epic 1 remains in-progress:
  1.1/1.2/1.3/1.4/1.5/1.6 done, only 1.7 remains.
  Previously: 2026-08-11 (Story 1.5 merged into the Epic 1 integration branch, joining Story 1.4. The two
  stories were developed on parallel branches and their artifacts were reconciled on merge: the
  sprint board now carries BOTH 1-4 and 1-5 as done, deferred-work.md carries both stories'
  sections, and Story 1.5's live-verification procedure — drafted as "P2" while Story 1.4 was in
  flight drafting its own "P2" — was renumbered to Procedure P3 in
  docs/qa/phase3-live-verification.md, since Story 1.4's account-gate procedure landed on P2 first.
  Story 1.5 artifacts that still say "Procedure P2" mean P3; the QA doc records the renumbering
  inline. Story 1.5 implemented -> code-reviewed -> done. Delivers REALTIME/RTH market-data
  configuration, the market-data line budget check, and the LiveBarObserver actor that subscribes,
  logs every bar with its instrument and timestamp, paces its subscriptions at the existing 45 req/s
  discipline, and notices a delayed feed. Split across src/core/live_market_data.py (pure policy) and
  src/core/live_bar_observer.py (the actor); live_node_builder.py gained bar_types/bar_observer and now
  passes market_data_type + use_regular_trading_hours explicitly with an AST guard proving it, since
  the adapter's own defaults happen to agree today and would otherwise mask a missing kwarg.
  93 new component tests, 5 unit, 1 integration; ruff + mypy clean. The three-layer adversarial
  review earned its keep twice over.
  (1) CRITICAL: the delayed-data guard would have shut down a HEALTHY session every RTH re-open — the
  IB adapter republishes the previous bar stamped with the current wall clock, so the first bar after
  an overnight gap looks ~17.5 hours late against a 180s threshold, and a multi-day paper session (the
  point of this phase) would have killed itself every morning. Fixed at the root by discarding
  republished bars, plus a consecutive-offender requirement as defence in depth. (2) CRITICAL: paced
  dispatch died silently after batch 2 on a real LiveClock — a fired one-shot timer is still listed
  while its callback runs, so re-arming the same name raised a KeyError that Nautilus swallows, while
  TestClock pops the timer first and kept all four pacing tests green. Fixed with a fresh timer name
  per batch and verified against a real LiveClock. Also found: ts_event is the bar's OPEN not its
  close, which made the documented "blind spot above ~13-minute intervals" simply false (the interval
  cancels; the guard works on hourly bars) — the story's own retraction is recorded rather than
  quietly corrected. Live evidence: the bars procedure ran against the paper Gateway on
  127.0.0.1:4002 and
  met pass criteria 1 and 2 (REALTIME confirmed in the gateway log, AAPL.NASDAQ qualified and loaded,
  subscription accepted, no IB 10167); criterion 3 (a bar actually delivered) is NOT met because the
  market was closed, and the probe reported that as a failure rather than printing ok — a re-run
  during RTH is logged as the outstanding item. Six items deferred, incl. the zero-delivery blind spot
  (-> Story 1.6, which owns the trading-permission flag) and LiveClock's cross-thread callbacks
  (-> Epic 2's runner). Note the story's own "the pre-existing 23-failure integration baseline is
  unchanged" line is superseded by the test-infra fix recorded below, which eliminated that baseline
  entirely. Epic 1: 1.1/1.2/1.3/1.4/1.5 done, 1.6/1.7 remain.
  Previously: 2026-08-11 (Test-infra fix, no story scope: the integration tier is deterministic again.
  `make test-integration` was reporting 23 failures out of 167 with a failing set that CHANGED
  between identical runs; every failure was "CRASHED with signal 5" (SIGTRAP). Root cause was the
  story-1.3 deferred item, and fixing it confirmed that entry was wrong twice over: the blast
  radius was not limited to TradingNode tests (all 23 crashes were BacktestEngine tests), and the
  "pre-existing 23-test baseline, unrelated to this mechanism" was in fact the same mechanism.
  `src/api/web.py` ran Nautilus `init_logging()` at MODULE IMPORT time; under `pytest -n auto
  --forked`, imports happen once per persistent xdist worker during COLLECTION, before any
  per-test fork, and `--forked` isolates only the test call — so every later forked child in a
  worker that had collected any `from src.api.web import app` test inherited a process image with
  logging already claimed, while the native threads the subsystem needs do not survive fork().
  Which tests crashed depended on how xdist distributed modules, hence the instability. Moved into
  a FastAPI `lifespan` hook guarded by `is_logging_initialized()` (never panics on re-entry, never
  overwrites an earlier caller's guard). Result: 169 passed / 2 skipped, identical across six
  consecutive runs; skips dropped 4 -> 2 because the two test_live_node_lifecycle.py tests now run
  instead of skipping. Verified the real server still works — uvicorn startup complete and GET /
  -> 200 — since lifespan, unlike import, only runs in the serving process. Regression guard:
  tests/component/api/test_web_app_logging.py (fresh-subprocess probe + a meta-test so it cannot
  pass vacuously). Also added an explicit AC -> test traceability table to story 1.4; all 8 ACs
  confirmed still satisfied, no source change to either gate module. Full suite green: 1588 unit,
  870 component, 169 integration, 192 api/ui, 1 e2e; ruff check + format + mypy clean.
  Epic 1 unchanged: 1.1/1.2/1.3/1.4 done, 1.5-1.7 backlog. Previously: 2026-08-09 (Story 1.4 implemented -> code-reviewed -> done. Layer 2 of the two-layer safety gate
  now exists, split along the purity line Story 1.1 established: the DECISION is
  evaluate_account_gate() in src/core/live_gate.py, still pure and still passing TestGatePurity's
  unchanged import whitelist; the ENFORCEMENT is a new src/core/live_account_gate.py that reads what
  the gateway actually reported, and on a refusal stops the node and raises the SAME GateRefusedError
  the static gate raises, so Story 1.7's exit-code-3 mapping already covers both layers. The safety
  property is structural rather than asserted: evaluate_account_gate STARTS by calling evaluate_gate
  and returns its refusal object unchanged, so Layer 2 can only ever be Layer-1-AND-more — six
  parametrised cases assert the propagated reason AND message are identical to Layer 1's. Three real
  holes closed: (1) a gateway that manages a real-money account ALONGSIDE the configured paper one
  passed both Layer 1 and the IB exec client's own membership check — Layer 2 now requires EVERY
  reported account to carry DU/DF; (2) the real-money crossing was previously compared config-to-config
  (NTRADER_REAL_MONEY_ACCOUNT vs TWS_ACCOUNT), two values that can agree with each other and disagree
  with the broker — it is now checked against what the gateway reports; (3) --real-money against a
  DU/DF demo account permitted mode=REAL_MONEY, which is the deferred-work item Story 1.1's review
  assigned to this story by name and is now struck through as RESOLVED. Case folding is deliberately
  ASYMMETRIC and commented as such: the paper-prefix test upper-cases (mirroring _evaluate_paper,
  which already permits a lowercase du...), the real-money identity test does not, because folding
  there would make more accounts match and on that path matching more means PERMITTING more.
  verify_connected_account is a coroutine and that is load-bearing — verified against the 1.220.0
  wheel that TradingNode.stop() merely SCHEDULES stop_async when the loop is running, and that
  dispose() busy-waits on a synchronous time.sleep that blocks the very loop the stop task needs
  (the deadlock Story 1.3's probe hit live on 2026-08-05); the seam awaits stop_async() and nothing
  else, leaving disposal and loop ownership with the caller per AR38. AR39's ordering is enforced at
  runtime rather than documented: with no runner in Epic 1, the seam refuses NODE_NOT_CONNECTED and
  STRATEGY_STARTED_BEFORE_ACCOUNT_GATE, the latter using an ALLOWLIST of not-yet-started states so a
  state Nautilus adds later reads as started and refuses. Code review by three adversarial layers
  produced 0 decisions / 17 patches / 8 deferred / 4 dismissed; all 8 ACs were confirmed satisfied and
  every figure in the Dev Agent Record was independently reproduced. The patches closed FOUR fail-open
  paths that lived inside satisfied ACs — an unguarded third-party attribute chain (node.kernel,
  node.trader, IB_CLIENTS, client.accounts()) whose AttributeError escaped with the node still up and
  connected; ExecutionEngine.check_connected() returning True vacuously on a node whose build() never
  ran; asyncio.CancelledError being a BaseException and so escaping except Exception to abort before
  the refusal was raised; and an unbounded post-refusal stop_async() that could withhold the verdict
  indefinitely. Also fixed: a case-folding comment that cited a rationale true only of the opposite
  branch, a log line rendering the UNSTRIPPED account set (IBKR's trailing-comma managedAccounts
  payload printed "accounts=, ***626"), masked duplicates rendering as "***567, ***567", third-party
  exception text reaching a log record (NFR26), AR41's gate.refused event name, AR36's gate-not-guard
  vocabulary, and four tests that asserted less than they claimed — including a determinism test that
  would have passed with sorted() deleted. Final: 1588 unit + 884 component collected, 2648 passed
  across unit/component/api/ui, 100% coverage on both changed modules, ruff/format/mypy clean,
  pyproject.toml and uv.lock byte-identical. ONE ITEM OUTSTANDING and recorded as non-blocking:
  Procedure P2 is fully documented but was NOT run against a live gateway — this worktree has no .env
  (gitignored and hook-protected) and the sandbox declined the invocations that would have supplied
  the settings another way, so per the QA doc's own policy the Result log says "not yet run" rather
  than claiming a pass. Eight items deferred, two of which Epic 2 must read before Story 2.5: the
  NautilusKernel.start_async() coroutine offers NO hook between "engines connected" and
  "trader started", so gate:account cannot be placed by polling once strategies exist (invisible in
  Epic 1 only because it configures zero strategies), and READY is reachable after a reset so the
  ordering guard has a blind spot across in-process session restarts. Epic 1 remains in-progress:
  1.1/1.2/1.3/1.4 done, 1.5-1.7 backlog. Previously: 2026-08-05 (Story 1.2 CLOSED -> done. Its last outstanding item, the .env.example
  client-ID wording, was rewritten under a one-off approved Bash write (.env* is Edit/Write-
  protected; the hook was left unchanged, per Story 1.1's precedent). The block now states the
  rotation range as base..base+5 and that the window MOVES WITH THE BASE (1-6 is what base 1
  happens to give, not a fixed range), replaces the superseded "must differ" rule with the real
  non-overlap-in-either-direction rule, names validate_client_ids_distinct as the enforcement
  point, and records the >= 1 floor (client ID 0 is IBKR's master client). All four claims in the
  new text were verified empirically against IBKRSettings rather than asserted: the file still
  constructs (1/10), base 5 is genuinely rejected, bases 1-4 are genuinely accepted, and client
  ID 0 is genuinely rejected. AC #4 now met across all four documented-configuration files.
  Epic 1 remains in-progress: 1.1/1.2/1.3 done, 1.4-1.7 backlog. Previously: Story 1.3 implemented → code-reviewed → done. src/core/live_node_builder.py delivers
  build_trading_node_config() and build_trading_node() exactly as designed: gate-first ordering
  (AC #2) proven by a monkeypatch sentinel test, not just "it raised"; both IB client configs
  carry ibkr_live_client_id never ibkr_client_id (AC #5); ibkr_read_only=False is a derived
  in-process view, proven never-branched-on by an AST check with its own meta-test (AC #3); the
  LogGuard registration/coexistence behaviour predicted by the story's pre-verification held
  exactly — BacktestEngine-then-TradingNode returns log_guard=None, TradingNode-first returns a
  real guard that set_nautilus_log_guard exposes (AC #4). 38 component tests (11 as delivered,
  27 more added by code review) and 2 integration tests, all TDD Red→Green. AC #7 clean (no dependency change). AC #6 IS verified
  live: a Gateway became available mid-session and Procedure P1 was run for real (see
  docs/qa/phase3-live-verification.md's Result Log), which surfaced and fixed a genuine
  loop-management bug in the probe script — two clean runs afterwards, exit 0, the second being
  the "no single-use carryover" evidence. One finding recorded in deferred-work.md rather than
  fixed (out of this story's scope): a pre-existing src/api/web.py module-import-time
  init_logging() side effect crashes this story's 2 integration tests with SIGTRAP, but only when
  tests/integration/api/test_trades_api.py is collected in the same pytest --forked run —
  confirmed by bisection across 7+ full runs to be a pre-existing test-infra hazard unrelated to
  live_node_builder.py's correctness (both tests pass 100% of the time in isolation and in every
  other file combination); Allay reviewed this finding directly and chose to document-and-proceed
  rather than fix src/api/web.py (off-limits for this story) or attempt a test-infra workaround.
  A second finding claiming the Task 6 test-count baselines were stale has been RETRACTED — it
  compared passed counts against collected baselines; 815 component and 169 integration were
  correct as collected counts (re-verified: 826 collected − 11 new = 815; 171 − 2 = 169).
  Code-reviewed 2026-08-05 → done: three adversarial layers produced 3 decisions / 26
  patches / 7 deferred / 5 dismissed. All three decisions ruled by Allay and applied. Highest-value
  catch was a Critical the story missed: an unvalidated trader_id containing no '-' panics in Rust
  and ABORTS the process (exit 134, uncatchable even by except BaseException), and it was
  reachable from the component tier — a bad parametrisation killed the pytest worker rather than
  failing a test. Now guarded with a legible LiveNodeConfigError, mirroring the TWS_ACCOUNT guard.
  Also fixed: build_trading_node now accepts and forwards an explicit event loop (it was silently
  binding to an ambient or auto-created one); a lowercase TWS_ACCOUNT passed the gate but reached
  the exec client un-normalised, which would have mismatched Story 1.4's Layer 2 check; non-positive
  IBKR timeouts flowed straight into both live clients; and the probe reported RESULT: ok without
  ever verifying the node connected (kernel.start_async does not raise on connection failure) while
  sleeping 5s against a 60s connection timeout. Previously: Story 1.3 created
  → ready-for-dev. First story that imports Nautilus. Its context was
  built by verifying the live surface against the installed nautilus-trader 1.220.0 wheel rather
  than from docs, which settled five things the epic could not: (1) TradingNodeConfig + IB client
  config assembly does NOT initialise the C logging subsystem, so config assembly is component-tier
  and only TradingNode construction needs --forked integration; (2) NautilusKernel already guards
  its own logging init and returns log_guard=None when a BacktestEngine got there first — verified
  BacktestEngine-then-TradingNode in one process with no panic, which is exactly AC #4's test;
  (3) get_cached_ib_client is keyed on (host, port, client_id), so the data and exec clients
  sharing ibkr_live_client_id share ONE socket by design — not the collision FR5 forbids;
  (4) the IB exec factory asserts on config.account_id or os.environ["TWS_ACCOUNT"], and .env never
  reaches os.environ, so account_id must be passed explicitly and an empty TWS_ACCOUNT — which the
  gate legitimately permits — must fail early with a legible error instead of a third-party
  AssertionError; (5) Nautilus has NO read-only knob for an operator-run gateway (read_only_api is
  DockerizedIBGatewayConfig-only), which confirms rather than contradicts NFR27/AR43: ibkr_read_only
  is a declaration of intent and the gate is the control. Two traps recorded that would each have
  cost a day: Environment.SANDBOX would silently replace IBKR execution with a local simulator, and
  a malformed trader_id panics in Rust rather than raising. One item flagged forward for Epic 3 —
  docker-compose.yml:51 hardcodes READ_ONLY_API "yes" on ib-gateway, harmless while no orders exist.
  Story 1.2 remains in-progress with one documentation-only item outstanding (the .env.example
  wording rewrite, blocked on hook approval); its values are already correct so it does not block
  1.3. Previously: Story 1.2 code-reviewed → in-progress. Three adversarial review layers produced 2 decisions / 8 patches / 5 deferred / 4 dismissed. Allay ruled on both decisions and they were applied TDD Red→Green: validate_client_ids_distinct was widened from bare equality to rotation-range intersection — it now rejects any overlap between the historical client's [base, base+5] and the reserved pair {live, live+1}, in both directions — and ge=1 was added to both fields because client ID 0 is IBKR's master client. le=999 was deliberately NOT added (repo convention, no basis in IBKR limits). A separate security finding was fixed: the new validator was rendering the entire environment into its traceback via pydantic's input_value=, leaking 22 of the 32 characters of FMP_API_KEY on every firing — closed with hide_input_in_errors. Two stale client-ID 10 defaults in scripts/ were also corrected, and the IBKR_SETUP.md guidance was self-contradictory across four places (a table saying 1–6, prose permitting 5–9, and a troubleshooting step saying ≤4) — now one rule, 1–4, plus an upgrade note for operators the old docs told to use 10. 1561 unit tests pass (1547 + 14 new). ONE ITEM OUTSTANDING: the .env.example wording rewrite is blocked by protect-files.sh and needs the same one-off approval Task 0 used; its current values 1/10 are verified valid, so this is wording only. Previously: Story 1.2 implemented → review. Task 0's blocking prerequisite was resolved first: .env:11 IBKR_CLIENT_ID 10 → 1, approved by Allay as a one-off shell write since .env* is hook-protected; its origin was traced to docs/setup/IBKR_SETUP.md:44, which is now corrected so the value cannot be reintroduced. Delivered: ibkr_live_client_id field + validate_client_ids_distinct model validator, 8 new unit tests (1547 total, baseline was 1539), reservation propagated to .env.example / docker-compose.yml / IBKR_SETUP.md / README.md, and the DataCatalogService unset-env fallback aligned to 1. Two items deferred under "Deferred from: story-1.2" — the os.environ-vs-typed-settings gap in DataCatalogService, and the fact that the validator compares configured bases only and so cannot see the historical client's 1-6 rotation range.)
project: Trading-ntrader
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ==========================================================================
  # PHASE 3 — Paper Trading (Live IBKR paper sessions + backtest comparison)
  # Source: _bmad-output/planning-artifacts/epics.md
  # Readiness: READY (implementation-readiness-report-2026-08-03.md — 0 critical, 0 major)
  #
  # Dependency chain (strictly linear, forward-only):
  #   E1 (connect + gate) → E2 (session identity, lifecycle, migration, trader_id)
  #     → E3 (orders + trade capture) → E4 (reconcile + resume) → E5 (seal + compare)
  #
  # Hard cross-epic constraint: E3 CANNOT start before E2 — deterministic client order
  # IDs (NFR6 duplicate-prevention) derive from the session-stable trader_id that E2's
  # Redis cache namespace establishes.
  #
  # Sequencing principles (do not rearrange): riskiest assumption first (the live IB
  # adapter is unproven in this repo, so E1 exercises it) and safety before capability
  # (the real-money gate ships before any order-submitting code path exists).
  #
  # Phase gate is MACHINERY-COMPLETE, not sample-complete: ≥1 round trip, 0
  # reconciliation discrepancy, ≥1 stop/restart with an open position, 1 sealed session
  # rendering in the existing views. Trade-sample accumulation is post-sign-off ops.
  #
  # epic-1..epic-5 keys are REUSED from Phases 1 and 2 but track DIFFERENT work.
  # ==========================================================================

  # Epic 1: Live Broker Connection & Real-Money Safety Gate
  # FR1–FR12 · Standalone: connects to IBKR paper, streams real-time RTH bars, refuses a
  # live account with a distinct exit code. No orders exist yet — that is the point.
  epic-1: done
  1-1-refuse-a-real-money-account-before-anything-connects: done
  1-2-isolate-the-live-client-id-from-the-historical-data-client: done
  1-3-assemble-and-start-a-tradingnode-against-ibkr-paper: done
  1-4-verify-the-connected-account-is-a-paper-account-before-trading-starts: done
  1-5-receive-real-time-rth-bars-for-a-configured-instrument: done
  1-6-detect-connection-loss-and-withhold-trading-permission: done
  1-7-check-broker-connectivity-and-the-gate-from-the-cli: done
  epic-1-retrospective: done

  # Epic 2: Paper Trading Sessions That Outlive the Process
  # FR13–FR22, FR48–FR53 · Standalone: a named session runs, observes bars, stops, and
  # restarts under the same identity. Still no orders.
  # Owns the SINGLE Alembic migration for the whole phase (Story 2.2): trading_sessions
  # (incl. G1 heartbeat cols), backtest_runs.run_type, trades.session_id,
  # trades.backtest_run_id → nullable + CHECK. run_type/session_id aren't consumed until
  # E5/E3 — deliberate, self-justified exception to epic-scoped schema change.
  epic-2: in-progress
  2-1-define-a-session-as-a-list-of-strategy-specifications: done
  2-2-create-a-named-session-with-an-immutable-specification: done
  2-3-move-a-session-between-states-through-one-guarded-path: done
  2-4-give-each-session-its-own-durable-engine-cache: done
  2-5-start-a-session-in-the-foreground-with-an-ordered-startup-sequence: done
  2-6-stop-a-session-without-ending-it-and-without-touching-positions: backlog
  2-7-keep-one-failing-strategy-from-taking-down-the-session: backlog
  2-8-see-what-a-session-is-doing-without-reading-logs: backlog
  epic-2-retrospective: optional

  # Epic 3: Live Order Execution & Trade Capture
  # FR23–FR31 · Standalone: the first complete round trip — entry submitted → filled →
  # position opened → exit filled → trade persisted with real fills and real commission.
  # BLOCKED ON E2's trader_id (deterministic client order IDs). Story 3.1 removes
  # close_all_positions() from on_stop() — must land before E4's stop/restart cycle, or
  # every daily stop manufactures a fake round trip.
  epic-3: backlog
  3-1-stop-trading-without-manufacturing-an-exit: backlog
  3-2-submit-a-strategys-orders-to-the-broker: backlog
  3-3-track-every-order-through-its-full-lifecycle: backlog
  3-4-never-resubmit-an-order-that-is-already-working: backlog
  3-5-aggregate-partial-fills-into-one-position: backlog
  3-6-persist-each-trade-the-moment-it-closes: backlog
  3-7-keep-trading-after-a-rejection: backlog
  epic-3-retrospective: optional

  # Epic 4: Stop, Restart & Resume Mid-Position
  # FR32–FR39 · Standalone: a session survives a full process stop/restart WHILE holding
  # an open position and resumes mid-position with warm indicators and broker-confirmed
  # state, no artificial exit. The phase's truth-test (external-authority check).
  epic-4: backlog
  4-1-read-the-brokers-authoritative-view-of-positions-and-cash: backlog
  4-2-reconcile-against-the-broker-before-any-strategy-trades: backlog
  4-3-keep-runtime-state-aligned-with-the-broker: backlog
  4-4-warm-indicators-from-history-before-the-live-stream-starts: backlog
  4-5-resume-a-strategy-mid-position: backlog
  4-6-check-positions-and-cash-against-ibkr-on-demand: backlog
  4-7-absorb-corporate-actions-through-broker-authoritative-state: backlog
  epic-4-retrospective: optional

  # Epic 5: Seal a Session & Compare It Against Its Backtest
  # FR40–FR47 · Standalone: a sealed paper session sits next to its backtest in the same
  # table, comparable at a glance, with ZERO UI changes — a success criterion, not an
  # omission. Scope discipline: any new metrics table/model/view invented for paper
  # results is an FR42 violation and must be rejected in review.
  epic-5: backlog
  5-1-compute-metrics-for-a-live-host-through-the-existing-extractor: backlog
  5-2-reconstruct-a-sealed-sessions-equity-curve-from-its-trades: backlog
  5-3-seal-a-stopped-session-into-a-comparable-run-record: backlog
  5-4-record-the-conditions-the-session-actually-ran-under: backlog
  5-5-handle-open-positions-at-seal-time-explicitly: backlog
  5-6-read-a-paper-session-beside-its-backtest-in-the-existing-views: backlog
  epic-5-retrospective: optional
